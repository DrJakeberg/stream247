Stream247
Owner · owner
WORKSPACES
Live covers control, status, and moderation for the current run.
Program owns schedule, pools, library, and sources. Studio owns scene, engagement, and output. Admin keeps settings, team access, and release posture aligned.
Live
Live · 128
Program
Studio
Admin
Open public page
Sign out
FEED
On air
idle
CURRENT
Abendprogramm — Folge 12
scheduled_match
NEXT
Abendprogramm
Queue will resolve on next cycle
DESTINATION
Primary Twitch Output
ready
INCIDENTS
0
No unresolved incidents
UPDATES
Polling
No worker heartbeat yet
Settings
Team
Settings
Manage workspace security, credentials, releases, and blueprints.
Settings change workspace-wide behavior: release posture, managed credentials, security defaults, and portable blueprints. Live channel actions stay in Live.
RELEASE
Update center
Keep production installs pinned to explicit image tags. Use the release preflight and upgrade rehearsal scripts before changing a live system.
Release channel
App <version> · channel custom · images aligned
Image tags
web: test
worker: test
playout: test
Production checklist
Images are pinned away from latest.
Something needed to go on air is missing.
Run `pnpm release:preflight` before upgrades and `pnpm release:rehearse vX.Y.Z` before major changes.
Runbooks
See `docs/upgrading.md`, `docs/backup-and-restore.md`, `docs/operations.md`, and `docs/versioning.md`.
CREDENTIALS
Managed credentials
Stream247 can now store integration credentials encrypted at rest in PostgreSQL. Environment variables still work as fallback values, but these settings let you manage Twitch and alert credentials from the UI.
TWITCH CLIENT ID
TWITCH CLIENT SECRET
DEFAULT TWITCH CATEGORY ID
DISCORD WEBHOOK URL
SMTP HOST
SMTP PORT
SMTP USER
SMTP PASSWORD
SMTP FROM
ALERT EMAIL TO
Save encrypted settings
SECURITY
Local account security
Optional two-factor authentication is available for local accounts. Team members signing in through Twitch SSO are not affected by this setting.
Status
Disabled.
CURRENT PASSWORD
Start two-factor setup
SETTINGS
Admin defaults
Twitch OAuth
Twitch OAuth is not ready until a client id and secret exist in settings or .env.
Alert delivery
Discord and SMTP alerts are currently unconfigured.
Fallback model
Empty fields do not wipe existing secrets. Stream247 keeps the stored value, or falls back to `.env` when no managed value exists.
Operational stance
Multi-output destinations, release preflight, upgrade rehearsal, and fresh-boot CI are now built into the default operating model.
PORTABILITY
Channel Blueprints
Channel Blueprints package Scene, sources, curated sets, program data, moderation, and destination routing metadata without exporting secrets, tokens, incidents, or media files.
Media binaries never travel with the blueprint. Insert assets, audio beds, cuepoint assets, and curated-set memberships only remap when matching media is already present locally in the target workspace.
Export channel blueprint
Import sections
Enabled sections replace the matching part of the current workspace. Leave a section disabled to keep the existing local state untouched.
Library sources and curated sets
Program pools, shows, and schedule
Scene live, draft, and presets
Moderation and destination routing
IMPORT BLUEPRINT JSON
LOAD BLUEPRINT FILE
Import blueprint