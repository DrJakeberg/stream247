# Gesamtprüfung Quelltext 2026-09-02 — aus dem Workflow-Journal rekonstruiert

Statuszeilen unter den bestätigten Befunden: Stand 13:00 UTC. v1.5.38 ist deployed (feec40c); alles danach liegt auf main und wartet auf die adversariale Gegenprüfung vor v1.5.39. Die 39 unverifizierten Befunde brauchen zuerst eine Gegenprüfung (Workflow), dann Triage.

Finder-Befunde gesamt: 51. **Bestätigt** (beide Gegenprüfer gelaufen, keiner widerlegt, Lauf 1): 12. Übrige: unverifiziert (Gegenprüfer am Limit gestorben), nicht widerlegt.

# Bestätigt

## [B1] HIGH / drift — EventSub alerts are registered for the moderator's own channel, never the broadcast channel — and reported as complete
**Status (2026-09-02 13:00 UTC):** IN ARBEIT — Agent-Worktree agent-a25d48ccae7b14ae8, 3 Commits (roter Test, Scope-Benennung, WIP), Fortsetzung ab 15:10
**Ort:** apps/worker/src/twitch-eventsub.ts:431

**Szenario:** Broadcast channel = jimpanse247, identity = 3jakec (moderator), alerts enabled. syncTwitchEventSubSubscriptions takes broadcasterId from state.twitch.broadcasterId, which apps/web/lib/server/twitch.ts:185-190 fills with the OAuth identity's own user id (3jakec). All four subscriptions (lines 157-187) are created with broadcaster_user_id=3jakec and moderator_user_id=3jakec, so follows/subs/cheers/redemptions on jimpanse247 never hit /api/overlay/events and nothing ever appears on air; meanwhile index.ts:7772 resolves the incident with 'Twitch EventSub configuration is complete' and an audit line says 'registered'. This is the one place the 'which channel' concept was not given the split treatment: live status (index.ts:7659), chat-settings (index.ts:7469) and the IRC bridge (twitch-engagement.ts:356) all resolve the broadcast channel via resolveBroadcastChannelLogin. Even with the id corrected, channel.subscribe / channel.cheer / channel_points require the channel owner to have granted channel:read:subscriptions / bits:read / channel:read:redemptions, and the broadcaster slot only asks for channel:manage:broadcast + channel:manage:schedule (packages/core/src/broadcast-channel.ts:52) — so three of the four alert types silently assume broadcaster rights the moderator setup never has.

**Beleg:**
```
const broadcasterId = args.state.twitch.broadcasterId.trim();
...
condition: (broadcasterId) => ({ broadcaster_user_id: broadcasterId, moderator_user_id: broadcasterId })
```
**Kopien:**
- apps/worker/src/index.ts:7659 (reconcileTwitchLiveStatus resolves broadcast channel login)
- apps/worker/src/index.ts:7469 (chat-settings resolves broadcast channel id via twitchUserIdResolver)
- apps/worker/src/twitch-engagement.ts:356 (IRC bridge joins the broadcast channel)
- apps/worker/src/index.ts:7734 (EventSub syncKey keys on state.twitch.broadcasterId, not the broadcast channel)

## [B2] HIGH / bug — Viewer !request cooldown and queue cap shown in the GUI are never enforced
**Status (2026-09-02 13:00 UTC):** ERLEDIGT — auf main (332aef3), noch nicht getaggt
**Ort:** apps/worker/src/index.ts:7866

**Szenario:** Viewer control has requests enabled; the GUI (apps/web/components/viewer-control-form.tsx:184 and :194) shows 'request cooldown 600 s' and a request queue limit. A viewer types '!request a', '!request b', '!request c' within five seconds. drainChatEffects calls evaluateViewerRequest with recentRequests: [] and queuedRequestCount: 0 on every call, so the cooldown branch (packages/core/src/chat-interaction.ts:409-426) and the queue-full branch (:404) can never fire; all three requests are appended to playout.queuedAssetIds (index.ts:7878-7881). Any single viewer can push an unbounded number of items into the running channel's queue while the settings page claims a per-viewer cooldown and a cap. The chat_viewer_requests table that would back this exists (packages/db/src/index.ts:3244) but nothing in the repo ever inserts into it.

**Beleg:**
```
recentRequests: [],
queuedRequestCount: 0,
```

## [B3] HIGH / bug — Emote-only is written to the broadcast channel every 30 s regardless of change or of the moderation 'enabled' switch
**Status (2026-09-02 13:00 UTC):** ERLEDIGT — auf main (7249db3), Gegenprüfung ab 15:10 vor v1.5.39
**Ort:** apps/worker/src/index.ts:7484

**Szenario:** Operator unchecks 'Enable moderation presence policy' (apps/web/components/moderation-settings-form.tsx:53) but leaves the default fallbackEmoteOnly=true (packages/core/src/index.ts:2104). resolveModeratorCheckIn now returns null for every check-in (core index.ts:2136), so no presence window ever exists; describePresenceStatus (core index.ts:2206-2233) reads only fallbackEmoteOnly and answers chatMode='emote-only'; reconcileTwitch then PATCHes helix/chat/settings with emote_mode:true as moderator on jimpanse247 on every 30 s cycle (index.ts:8204) while anything is on air. The channel's own moderators cannot keep emote-only off for more than 30 s. The mirror case is just as real: with a window active or the fallback off, emote_mode:false is forced every 30 s, silently undoing a human moderator who enabled emote-only for a raid. The write is unconditional — there is no compare-to-last-written and no gate on state.moderation.enabled.

**Beleg:**
```
const chatResponse = await fetch(`https://api.twitch.tv/helix/chat/settings?broadcaster_id=...&moderator_id=...`, { method: "PATCH", ..., body: JSON.stringify({ emote_mode: presenceStatus.chatMode === "emote-only" }) });
```

## [B4] HIGH / silent — 'Enable chat overlay' silently switches off moderator check-ins, votes, skip votes, requests and the chat game
**Status (2026-09-02 13:00 UTC):** IN ARBEIT — Worktree chat-bridge-gate, Core+Bridge+Beschriftung, Validierung/Baselines/Smoke laufen
**Ort:** apps/worker/src/twitch-engagement.ts:362

**Szenario:** Operator unchecks 'Enable chat overlay' (apps/web/components/engagement-settings-form.tsx:88-93, described as 'the on-stream Twitch chat rail') to hide the chat panel. TwitchChatBridge.sync gates the entire IRC connection on isEngagementChatRuntimeEnabled and calls disconnect('disabled'). The bridge is the only intake for moderator presence check-ins (onModeratorPresenceCheckIn, index.ts:379), votes/skip/requests (chatControl.handleMessage, index.ts:403) and the game (index.ts:425). A moderator types '!here 60' in jimpanse247 chat: no reply, no presence window; once the last window expires the emote-only fallback engages. The Moderation page still shows the policy enabled with command 'here', and Viewer Control still shows voting/skip/requests enabled — none of it works, and no incident says why.

**Beleg:**
```
const enabled = isEngagementChatRuntimeEnabled(state.engagement, env, state.managedConfig);
...
if (!enabled || !nick || !channel || !accessToken) {
  await this.disconnect("disabled");
  return;
}
```

## [B5] MEDIUM / drift — 'Active chatters' is counted twice with different windows: the GUI number and the skip threshold disagree
**Status (2026-09-02 13:00 UTC):** ERLEDIGT — v1.5.38
**Ort:** apps/worker/src/chat-control.ts:40

**Szenario:** Engagement game window set to 15 min (allowed range 1-30, packages/core/src/index.ts:388). EngagementGameTracker (engagement-game.ts:99-107) counts chatters over that window and the overlays page prints it: 'live with 12 active chatters in the last 15 minutes' (apps/web/app/(admin)/overlays/page.tsx:95, fed by apps/web/lib/server/state.ts:1006). ChatControlRuntime counts chatters over a hard-coded 5-minute window (ACTIVE_CHATTER_WINDOW_MS) and applySkipVote derives votesNeeded from that count (packages/core/src/chat-interaction.ts:290-292). With 4 chatters in the last 5 min and threshold 60 %, the on-air panel says '3 von 3 Stimmen' and the item is skipped, while the admin GUI is telling the operator 12 viewers are active (which would have needed 8). chat-overlay.ts:28 carries a third literal copy of the 5-minute value with a comment asserting it equals ACTIVE_CHATTER_WINDOW_MS, so a change to either constant silently breaks that claim.

**Beleg:**
```
const ACTIVE_CHATTER_WINDOW_MS = 5 * 60_000;
```
**Kopien:**
- apps/worker/src/engagement-game.ts:99 (window = getEngagementGameWindowMs(settings), operator-configurable)
- apps/worker/src/chat-overlay.ts:28 (CHAT_OVERLAY_MESSAGE_TTL_MS = 5 * 60_000, comment claims parity with ACTIVE_CHATTER_WINDOW_MS)

## [B6] MEDIUM / bug — A persistent Twitch reconcile failure sends a Discord post and an email every 30 s with no dedupe
**Status (2026-09-02 13:00 UTC):** ERLEDIGT — auf main (ecb15c7), Gegenprüfung ab 15:10
**Ort:** apps/worker/src/index.ts:7065

**Szenario:** The identity account loses moderator status on jimpanse247 (or the broadcast channel login in Settings has a typo, so twitchUserIdResolver.resolve throws 'returned no user' at twitch-broadcast-channel.ts:49 every cycle). sync() throws 'Chat settings sync failed with status 403.'; the catch at index.ts:7586-7596 calls upsertIncident — which dedupes by fingerprint — and then sendAlert, which does not: sendDiscordAlert (7015) and sendEmailAlert (7033) fire unconditionally on every call. The worker cycle is 30 s (index.ts:8204), so the operator receives ~120 Discord messages and ~120 emails per hour for as long as the condition persists. Same path for schedule-sync failures at 7646.

**Beleg:**
```
async function sendAlert(subject: string, message: string): Promise<void> {
  await Promise.allSettled([sendDiscordAlert(`Stream247: ${message}`), sendEmailAlert(subject, message)]);
}
```

## [B7] HIGH / drift — The on-air scene payload is assembled twice: the GUI/public snapshot builds it in the web, the stream builds it in the worker, and the two disagree on next item, title, meta line and time label
**Status (2026-09-02 13:00 UTC):** OFFEN — gehört zu Overlay Etappe 3 (ein Payload-Aufbau in core)
**Ort:** apps/web/lib/server/state.ts:1062

**Szenario:** After the last schedule block of the day: the web copy uses findNextScheduleOccurrenceAcrossDays (state.ts:376), so the Live page, /api/broadcast/state, /api/channel/live, the /overlay browser page and /api/scenes all show tomorrow's first block as 'Next' with '06:00 to 08:00'; the worker copy uses same-day findNextScheduleOccurrence (apps/worker/src/index.ts:3916) and the frame ffmpeg actually receives says 'Coming up next' / 'No next block configured'. While a chaptered asset is on air the stream hero shows the chapter title (index.ts:2847 resolveOnAirChapterTitle) but the GUI shows the asset title (the web chain has no chapter step). With no category the GUI meta line reads 'Always on air' (state.ts:1100 passes it as currentCategory) while on air the meta line omits it (worker passes undefined). Time labels differ everywhere: web '20:00 to 22:00' (state.ts:1106) vs worker '20:00-22:00' (index.ts:2854). Operator sees an overlay in the studio/live page that the stream does not show — the exact shape already complained about.

**Beleg:**
```
state.ts:1093-1108: target: options.target ?? "browser", ... nextTitle: nextAssetTitle || nextScheduleLookaheadTitle || state.playout.nextTitle || nextScheduleItem?.title || "Schedule not available", nextTimeLabel: nextScheduleItem ? `${nextScheduleItem.startTime} to ${nextScheduleItem.endTime}` : ... currentCategory: ... || "Always on air"  —  apps/worker/src/index.ts:2845-2854: currentTitle: overrides.currentTitle || resolveOnAirChapterTitle(state, asset) || buildAssetDisplayTitle(asset) || state.playout.currentTitle || currentItem?.title || "Stand by", nextTitle: overrides.nextTitle || nextItem?.title || "Coming up next", nextTimeLabel: ... `${nextItem.startTime}-${nextItem.endTime}`
```
**Kopien:**
- apps/worker/src/index.ts:2692 (buildWorkerScenePayload)
- apps/worker/src/index.ts:2818 (writeOnAirOverlay — the payload that reaches ffmpeg)
- apps/worker/src/index.ts:2774 (writeStandbySlate: currentItem?.title || "Stand by" vs state.ts standby chain queueHead?.title || currentTitle || overlay.headline || "Replay stream")
- apps/worker/src/index.ts:3901 (getNextScheduleItem, same-day) vs apps/web/lib/server/state.ts:375 (across days)
- apps/web/app/api/scenes/route.ts:27 (studio 'live scene' is the web copy, not the worker's)
- apps/web/components/overlay-settings-form.tsx:360 (third, client-side copy with target "browser")

## [B8] HIGH / drift — Custom-layer placement, safe area and overlay scale are implemented in the web separately from the renderer that goes on air (packages/core/overlay-layout.ts) with different clamps and margins
**Status (2026-09-02 13:00 UTC):** TEILWEISE — Browser-Kopie in Etappe 2 gelöscht; ein Platzierungsmodell = Etappe 3
**Ort:** apps/web/lib/overlay-layout.ts:38

**Szenario:** Operator drags a logo layer to xPercent 0, widthPercent 5 at 1920x1080. The studio canvas (overlay-scene-canvas.tsx:159) and the /overlay browser page (live-overlay.tsx:28, fed by getPublicChannelSnapshot) draw a 5%-wide box starting 5% from the left edge (globals.css --safe-area-left-percent: 5%, width clamp min 0). The satori frame piped to ffmpeg draws a 10%-wide box starting 72px = 3.75% in (core resolvePlacementBox: safeX px(72), widthPercent clamped to min 10, heightPercent min 8). At 1280x720 the web imitation scales everything by min(1,max(0.62,720/720)) = 1.0 while the renderer scales by max(0.35,1280/1920) = 0.667 — every panel in the GUI is 50% larger than on air. The 'real overlay next to the imitation' commit left the imitation and its private geometry in place.

**Beleg:**
```
apps/web/lib/overlay-layout.ts:10: const overlayScale = Math.min(1, Math.max(0.62, outputHeight / 720));  :40-42: const widthPercent = clampPercent(layer.widthPercent, 0, 100 - xPercent); const heightPercent = clampPercent(layer.heightPercent, 0, 100 - yPercent);  :55: left: `calc(var(--safe-area-left-percent) + ...`  —  packages/core/src/overlay-layout.ts:42: return Math.max(0.35, width / 1920);  :717-724: const safeX = placement.allowOutsideSafeArea ? 0 : px(72); const safeY = ... px(56); ... clampPlacementPercent(placement.widthPercent, 10, 100 - xPercent); clampPlacementPercent(placement.heightPercent, 8, 100 - yPercent);
```
**Kopien:**
- packages/core/src/overlay-layout.ts:42 (overlayScale — the on-air scale)
- packages/core/src/overlay-layout.ts:709 (resolvePlacementBox — the on-air clamp and safe area)
- apps/web/app/globals.css:29-36 (--safe-area-*-percent 5% / --overlay-safe-area-*-percent 90%)
- apps/web/components/overlay-scene-canvas.tsx:159 (studio imitation using the web clamp)
- apps/web/components/live-overlay.tsx:28 (/overlay browser page using the web scale)

## [B9] CRITICAL / silent — Every decryption failure is swallowed: a lost/rotated app secret makes the whole install read as 'unconfigured' and bypasses 2FA, with no log anywhere
**Status (2026-09-02 13:00 UTC):** ERLEDIGT — auf main (667e7b2), Gegenprüfung ab 15:10 (Sicherheit)
**Ort:** packages/db/src/index.ts:1454

**Szenario:** The media volume is recreated, MEDIA_LIBRARY_ROOT is changed, or APP_SECRET is set later than the first boot. packages/db/src/app-secret.ts:85-88 then silently generates and persists a NEW secret (no log), so every AES-GCM ciphertext in Postgres fails its auth tag. decryptSecretString returns "" and decryptManagedConfig returns null, and readAppState (line 4861-4866) substitutes defaults.managedConfig. What the operator sees: the Twitch client id/secret, broadcast channel login (jimpanse247) and public URL are blank in Settings as if never entered; every destination flips to 'missing-config' with the note 'Save a managed stream key' (line 5016 / 6994 read ""); the uplink raises 'Persistent uplink destination is not configured' and stops publishing; reconcileTwitch drops to the env fallback client id; the relay publish key (7308) and live-bridge URL (7174) vanish. Worse: two_factor_secret decrypts to "" (line 4828) and apps/web/app/api/auth/login/route.ts:33 gates the TOTP step on `user.twoFactorEnabled && user.twoFactorSecret`, so 2FA is silently skipped for every local login while the user record still says 2FA is on. Not one runtime event, incident or audit line says 'decryption failed / key mismatch'. Should see: a logged event and a critical incident ('managed secrets cannot be decrypted with the current APP_SECRET') the first time a non-empty ciphertext fails, and a hard refusal (not a bypass) on the 2FA gate.

**Beleg:**
```
function decryptSecretString(value: string): string {
  ...
  try {
    const decipher = createDecipheriv("aes-256-gcm", getEncryptionKey(), Buffer.from(ivText, "base64url"));
    ...
  } catch {
    return "";
  }
}

function decryptManagedConfig(value: string): ManagedConfigRecord | null {
  ...
  } catch {
    return null;
  }
}

// readAppState, line 4861:
    managedConfig: decryptedManagedConfig ? { ...decryptedManagedConfig, ... } : defaults.managedConfig,

// apps/web/app/api/auth/login/route.ts:33
  if (user.authProvider === "local" && user.twoFactorEnabled && user.twoFactorSecret) {
```

## [B10] HIGH / silent — Moderator !here check-in: the chat reply confirms the window before it is persisted, and the persistence failure is swallowed
**Status (2026-09-02 13:00 UTC):** ERLEDIGT — auf main (3500e98), Gegenprüfung ab 15:10
**Ort:** apps/worker/src/twitch-engagement.ts:560

**Szenario:** A moderator (3jakec) types '!here 30' in jimpanse247's chat. Line 551-559 immediately sends the clamp reply ('checked in for 30 minutes'). Line 560 then fires onModeratorPresenceCheckIn and discards any rejection. The handler in apps/worker/src/index.ts:379-392 does two DB writes (appendPresenceWindowRecord, appendAuditEvent) with no try/catch of its own. If the write fails (pool exhausted, advisory-lock timeout, transient Postgres error), the moderator has been told they are checked in, but no presence window exists: describePresenceStatus in reconcileTwitch keeps emote-only fallback exactly as before, the Moderation page shows no active window, and the audit trail has no checkin entry. Nothing is logged — not even a runtime event. Should see: a runtime event (e.g. moderation.checkin.persist_failed) and an incident, and ideally the chat reply sent only after the record is written.

**Beleg:**
```
// twitch-engagement.ts:551-560
        this.sendChatMessage(
          formatPresenceClampReply({ ... })
        );
        void Promise.resolve(this.onModeratorPresenceCheckIn?.(presenceWindow)).catch(() => undefined);
        continue;

// apps/worker/src/index.ts:379-392
  async onModeratorPresenceCheckIn(window) {
    await appendPresenceWindowRecord({ ... });
    await appendAuditEvent("moderation.checkin", `${window.actor} checked in for ...`);
  },
```

## [B11] MEDIUM / silent — Alert delivery never reports failure: Discord response status is unchecked, both channels' errors are dropped by allSettled, and 'nothing configured' returns silently
**Status (2026-09-02 13:00 UTC):** ERLEDIGT — auf main (ecb15c7), Gegenprüfung ab 15:10
**Ort:** apps/worker/src/index.ts:7022

**Szenario:** The Discord webhook is deleted on the Discord side (404), or SMTP credentials stop working, or the operator never configured either. Every critical path (Twitch reconcile failure, token refresh failure, schedule sync failure) calls sendAlert, which does Promise.allSettled and throws the results away (line 7066). sendDiscordAlert awaits fetch without reading response.ok and its catch is empty (7022-7030); sendEmailAlert returns silently when host/port/from/to are missing (7041-7043) and its rejection lands in the discarded allSettled result. The operator believes alerts are on (the settings show a webhook URL) while nothing has been delivered for hours; there is no runtime event, no incident and no 'last alert delivered at'. Should see: a runtime event on each non-2xx/rejected delivery and an incident 'alerts are not reaching any channel' after the first failure; a one-time incident when an alert is raised with no channel configured.

**Beleg:**
```
async function sendDiscordAlert(message: string): Promise<void> {
  ...
  if (!webhookUrl) {
    return;
  }
  try {
    await fetch(webhookUrl, { method: "POST", ... });
  } catch {
    // Alert delivery errors should not crash the worker loop.
  }
}
...
  if (!host || !port || !from || !to) {
    return;
  }
...
async function sendAlert(subject: string, message: string): Promise<void> {
  await Promise.allSettled([sendDiscordAlert(`Stream247: ${message}`), sendEmailAlert(subject, message)]);
}
```

## [B12] HIGH / drift — The /overlay browser page is still advertised as the captured overlay output, but nothing captures it - it is the third, divergent drawing
**Status (2026-09-02 13:00 UTC):** ERLEDIGT — Overlay Etappe 2 (v1.5.38)
**Ort:** apps/web/app/(admin)/overlay-studio/page.tsx:37

**Szenario:** Operator reads "Stream247 captures https://.../overlay as its internal overlay output" on the Scene tab and the dashboard, clicks "Internal overlay output -> Open overlay page" to check what viewers see, and gets the HTML rebuild: 5% safe area (renderer uses 72/1920 by 56/1080), iframe embeds/widgets, follow/sub alerts, the chatter-participation game, a clock with seconds and zone name - none of which the satori renderer puts on air. The worker never fetches this page: the base-URL resolver exists but has no caller and there is no Chromium/puppeteer/playwright in apps/worker/src.

**Beleg:**
```
overlay-studio/page.tsx:37-39 `Stream247 captures <code>{getAbsoluteAppUrl(state, "/overlay")}</code> as its internal overlay output.` and 118-123 `<strong>Internal overlay output</strong> ... <Link href="/overlay">Open overlay page</Link>`; dashboard/page.tsx:328-331 repeats it. The page draws the imitation: live-overlay.tsx:30-31 `<OverlaySceneCanvas payload={snapshot.activeScenePayload} /><EngagementOverlay initialEngagement={snapshot.engagement} />` with its own safe area (globals.css:1627-1630 `--safe-area-top: calc(var(--overlay-height) * 0.05)`; apps/web/lib/overlay-layout.ts:39-42 clamps width 0..100-x, opacity 0..100) versus the renderer's (packages/core/src/overlay-layout.ts:717-725 `px(72)`/`px(56)`, width min 10, height min 8, opacity min 5). Worker: apps/worker/src/scene-renderer.ts:3-7 "This replaces on-air-scene.ts, which launched a full Chromium process per frame to screenshot the /overlay page"; `getSceneRendererBaseUrl` is defined at on-air-scene.ts:51 and `grep -rn getSceneRendererBaseUrl apps/worker/src` finds no call site; `grep -rl 'puppeteer|playwright|chromium' apps/worker/src` is empty.
```
**Kopien:**
- apps/web/app/overlay/page.tsx:17-22 and apps/web/components/live-overlay.tsx:25-33 (third drawing, served and linked)
- apps/web/lib/overlay-layout.ts:38-61 (browser copy of the placement clamp)
- apps/web/app/globals.css:1621-1667 (browser copy of the safe area)
- packages/core/src/overlay-layout.ts:710-733 and 1300 (renderer placement clamp and padding)
- apps/web/app/(admin)/dashboard/page.tsx:328-331 (same capture claim)
- apps/web/app/(admin)/overlays/page.tsx:40 and apps/web/components/engagement-settings-form.tsx:89 ("captured overlay" claim)

# Unverifiziert

## [U1] HIGH / silent — Engagement alerts (EventSub) are registered for the connected identity's own channel, not the configured broadcast channel; the webhook and the GUI snapshot never check which channel an event is for
**Ort:** apps/worker/src/twitch-eventsub.ts:318

**Szenario:** Identity connected as moderator 3jakec, twitchBroadcastChannelLogin = jimpanse247, alerts enabled. The worker creates channel.follow/subscribe/cheer/channel_points subscriptions with broadcaster_user_id = 3jakec's id (and moderator_user_id = the same id). A viewer follows or subscribes on jimpanse247 -> nothing ever arrives, no alert on air. Someone follows 3jakec's private channel -> /api/overlay/events accepts the notification (no comparison of event.broadcaster_user_login with the broadcast channel), appends it, and the alert is drawn onto jimpanse247's stream. Meanwhile the Live page's engagement summary reports alertsRuntimeEnabled: true and chatStatus from the same identity, so the GUI says alerts are live for a channel they are not attached to. The metadata sync and live-status polling were already split-aware; alerts were not.

**Beleg:**
```
apps/worker/src/twitch-eventsub.ts:318: const broadcasterId = args.state.twitch.broadcasterId.trim();  :48-50: condition: (broadcasterId) => ({ broadcaster_user_id: broadcasterId, moderator_user_id: broadcasterId })  —  apps/web/app/api/overlay/events/route.ts:149-151: const actor = payload.event?.user_name || ...; const event = await appendEngagementEventRecord({ ... })  (no broadcaster check)  —  apps/web/lib/server/state.ts:963: const alertsRuntimeEnabled = isEngagementAlertsRuntimeEnabled(engagement, process.env, managedConfig);
```

## [U2] MEDIUM / bug — Runtime drift report flags 'Twitch metadata does not match the active schedule block' by comparing the synced title against the block title, but the worker syncs an asset/chapter-derived title
**Ort:** apps/web/lib/server/state.ts:1501

**Szenario:** Block 'Evening Replays' (category Just Chatting) plays pool asset 'Match 12' with titlePrefix 'Replay:' and hashtags. Worker computes desiredTitle = buildTwitchMetadataTitle(asset, ...) = 'Replay: Match 12 #foo' and category from the asset/chapter first (apps/worker/src/index.ts:7328-7333), PATCHes Twitch successfully and records lastSyncedTitle = 'Replay: Match 12 #foo'. The Live page drift panel then shows a permanent warning 'Twitch metadata does not match the active schedule block yet' (and counts it in attentionCount) for as long as anything from a pool is on air — i.e. always. With a broadcast-channel split waiting for the broadcaster slot the same item warns with 'Last synced: none' and the only hint text ('Connect the broadcaster account') is gated on state.twitch (the moderator identity), not on state.twitchBroadcaster, which is the slot the worker actually gates writes on (index.ts:7350-7355).

**Beleg:**
```
state.ts:1499-1503: state.twitch.status !== "connected" ? "info" : currentScheduleItem && (state.twitch.lastSyncedTitle !== currentScheduleItem.title || (currentScheduleItem.categoryName !== "" && state.twitch.lastSyncedCategoryName !== currentScheduleItem.categoryName)) ? "warning" : "ok"  —  apps/worker/src/index.ts:7328: const desiredTitle = metadataAsset ? buildTwitchMetadataTitle(metadataAsset, currentScheduleItem?.title || state.playout.currentTitle) : ...  —  apps/worker/src/twitch-metadata.ts:22-28: title = [titlePrefix, baseTitle, ...hashtags].join(" ").slice(0,140)
```

## [U3] MEDIUM / drift — Playout heartbeat staleness threshold is 45 s in the Live page drift report but 60 s in readiness/health and in the worker
**Ort:** apps/web/lib/server/state.ts:1446

**Szenario:** Playout heartbeat is 50 s old with status 'running'. The Live page runtime drift panel shows 'Playout heartbeat is stale' as a warning and bumps attentionCount, while /api/health, /api/ready and /api/system/readiness (getSystemReadiness, readiness.ts:97) report playout 'ok' and overall status 'ok', and the worker itself (PLAYOUT_HEARTBEAT_STALE_MS = 60_000) has not yet reacted. The operator sees the GUI and the monitoring endpoint disagree about whether playout is stalled during exactly the 15 s window that matters. WORKER_HEARTBEAT_STALE_MS is likewise copied three times (currently equal).

**Beleg:**
```
state.ts:1446: ? playoutHeartbeatAgeMs > 45_000  —  apps/web/lib/server/readiness.ts:97: if (effectivePlayoutHeartbeat > 0 && now - effectivePlayoutHeartbeat < 60_000) {  —  apps/worker/src/index.ts:352: const PLAYOUT_HEARTBEAT_STALE_MS = 60_000;
```
**Kopien:**
- apps/web/lib/server/readiness.ts:97 (60_000 literal)
- apps/web/lib/server/readiness.ts:7 (RUNTIME_HEARTBEAT_STALE_MS = 60_000)
- apps/worker/src/index.ts:352 (PLAYOUT_HEARTBEAT_STALE_MS = 60_000)
- apps/web/lib/server/state.ts:1398 (WORKER_HEARTBEAT_STALE_MS = 240_000)
- apps/web/lib/server/readiness.ts:6 (WORKER_HEARTBEAT_STALE_MS = 240_000)
- apps/worker/src/index.ts:350 (WORKER_HEARTBEAT_STALE_MS = 240_000)

## [U4] LOW / drift — queuePreviewCount is clamped in three places with two different limits: blueprint import allows 1..8, the overlay API and the DB read path allow 1..5
**Ort:** apps/web/lib/server/channel-blueprints.ts:274

**Szenario:** Operator imports a channel blueprint whose overlay has queuePreviewCount 8. normalizeOverlaySettings in the blueprint importer accepts 8, updateAppState/writeAppState stores 8 verbatim (packages/db/src/index.ts:3780 writes next.overlay.queuePreviewCount unnormalised), the import audit/review reports the imported overlay with 8, but every readAppState re-normalises through normalizeOverlaySettingsRecord (db:948 -> :892) to 5, so the studio shows 5, the worker slices queueTitles to 5, and a re-export writes 5. The GUI reported an import that the product did not apply.

**Beleg:**
```
channel-blueprints.ts:272-275: queuePreviewCount: typeof candidate.queuePreviewCount === "number" && Number.isFinite(candidate.queuePreviewCount) ? Math.max(1, Math.min(8, Math.round(candidate.queuePreviewCount))) : fallback.queuePreviewCount  —  packages/db/src/index.ts:892: queuePreviewCount: Math.max(1, Math.min(5, Number(overlay.queuePreviewCount ?? defaults.queuePreviewCount) || defaults.queuePreviewCount))  —  apps/web/app/api/overlay/route.ts:81: queuePreviewCount: Math.max(1, Math.min(5, Number(payload.queuePreviewCount ?? base.queuePreviewCount) || 3))
```
**Kopien:**
- packages/db/src/index.ts:892 (normalizeOverlaySettingsRecord, 1..5, applied on read at :948)
- apps/web/app/api/overlay/route.ts:81 (sanitizeOverlayPayload, 1..5)
- packages/db/src/index.ts:3780 (writeAppState stores the value without normalising)

## [U5] MEDIUM / silent — Local-library thumbnail generation fails silently: ffmpeg/stat errors return false and the caller discards the result
**Ort:** apps/worker/src/asset-thumbnails.ts:125

**Szenario:** The thumbnails directory under MEDIA_LIBRARY_ROOT is not writable by the worker user, or ffmpeg is missing / a file has no decodable video in its first second. ensureLocalAssetThumbnail catches every error and returns false (line 103 for the stat pair, line 125-128 for the ffmpeg run); syncLocalMediaLibrary at apps/worker/src/index.ts:3068-3072 awaits it and ignores the boolean. The Library page shows the generic fallback SVG for every asset (apps/web/lib/server/asset-thumbnails.ts:20-24 returns null and the route serves the placeholder) on every cycle forever; grep finds no logRuntimeEvent or incident mentioning thumbnails anywhere in the worker. The operator sees 'no artwork' and nothing says why. Should see: a runtime event with the asset id and the ffmpeg stderr on failure, and an incident once the failure repeats for the same asset.

**Beleg:**
```
  } catch {
    return false;
  }

  try {
    await execFileAsync(args.ffmpegBinary || process.env.FFMPEG_BIN || "ffmpeg", [ ... tempPath ]);
    await fs.rename(tempPath, targetPath);
    return true;
  } catch {
    // Only the incomplete render is removed. Whatever was already on the final path stays.
    await fs.unlink(tempPath).catch(() => {});
    return false;
  }

// apps/worker/src/index.ts:3068
  for (const asset of nextAssets) {
    await ensureLocalAssetThumbnail({ assetId: asset.id, inputPath: asset.path, mediaRoot });
  }
```

## [U6] MEDIUM / silent — Chat bridge reports 'disabled' when the real blocker is a missing Twitch token or channel login
**Ort:** apps/worker/src/twitch-engagement.ts:362

**Szenario:** Operator switches chat on in Engagement settings while the Twitch connection is in 'error' (expired/revoked token → accessToken "" after a failed refresh) or before the identity login is stored. sync() folds four different reasons into one branch: `!enabled || !nick || !channel || !accessToken` → disconnect("disabled") → setPhase("idle") and a status event 'disconnected: disabled' (lines 456-464). The engagement event list shows 'Chat idle / disabled' even though chat is enabled in the settings the operator just saved; the chat overlay row is written empty; no runtime event names the missing token or channel. Meanwhile the login-rejected path (line 373-376) gets an incident, so this is the one chat-inert condition the operator cannot see. Should see: a distinct phase/status ('waiting for Twitch connection' / 'no broadcast channel login') and a runtime event naming which of the four inputs was empty.

**Beleg:**
```
    if (!enabled || !nick || !channel || !accessToken) {
      await this.disconnect("disabled");
      return;
    }
...
    if (reason === "disabled") {
      ...
      this.setPhase("idle");
      await appendChatStatus("disconnected", "disabled");
    }
```

## [U7] LOW / silent — Accepted chat messages can vanish from the engagement event log with no trace
**Ort:** apps/worker/src/twitch-engagement.ts:588

**Szenario:** A chat line passes the display limiter and is pushed into the in-memory overlay buffer (line 586-587), then appendEngagementEventRecord(event) is fired and its rejection dropped. If Postgres refuses the insert (pool exhausted during a heavy sync, or a transient connection error), the message reaches the on-air chat row via the overlay flush — whose failure IS logged as chat.overlay.flush_failed (index.ts:556-560) — but never reaches engagement events, so the dashboard's recent-events list and moderation review disagree with what viewers saw on air, with no runtime event explaining the gap. Should see: the same failure logging the overlay flush already does (e.g. chat.event.append_failed with the message id).

**Beleg:**
```
      this.messages.push({ ...event, login: message.login });
      this.onOverlayMessagesChanged?.();
      void appendEngagementEventRecord(event).catch(() => undefined);
```

## [U8] HIGH / drift — Scene visibility toggles and layer order are drawn by the studio rebuild but never read by the on-air renderer
**Ort:** apps/web/components/overlay-settings-form.tsx:1314

**Szenario:** Operator unchecks "Show next item" and "Show clock" (or presses "Hide layer" on Clock in "Scene layer order"), types a ticker, reviews and publishes. The publish review lists "Next item: shown -> hidden", "Clock: shown -> hidden", "Ticker: none -> ..." as going live and the studio rebuild hides both cards. On air the clock chip is still drawn, the next card is still drawn - and on a standby channel with an empty queue it literally reads "NEXT · NOTHING SCHEDULED NEXT / Schedule not available" because the payload builder never lets nextTitle be empty - and the ticker never appears (only the SCENE_RENDERER_ENABLED=0 drawtext fallback prints it). Same for "Show queue preview"/"Queue preview count" and "Show schedule teaser": queueTitles and schedule* are never read by the layout.

**Beleg:**
```
Form (overlay-settings-form.tsx:1310-1350): checkboxes showClock/showNextItem/showScheduleTeaser/showQueuePreview, queuePreviewCount; layer order/hide buttons at 754-785. Core builds the gates into scene.layers (packages/core/src/index.ts:1698-1720: `next: args.overlay.showNextItem, queue: ..., schedule: ..., clock: args.overlay.showClock ... enabled: enabledMap[kind] && !disabledLayers.has(kind)`). The on-air layout has zero references to `.layers`, `disabledLayers` or `layerOrder` (grep of packages/core/src/overlay-layout.ts is empty); `tickerText` and `queueTitles` occur only in the type (lines 225-226). Clock is unconditional: overlay-layout.ts:1221-1239 `const clockChip = label(clock, ...); const topBar = [row(..., [banner ? buildBanner(...) : {...}, clockChip])]`. Next card is drawn whenever nextTitle is non-empty (1171, 1270-1272 `else if (nextCard) rail.push(nextCard)`), and core index.ts:1812 makes it never empty: `nextTitle: nextTitle || "Schedule not available"`. normalizeScenePreviewRequest (apps/web/lib/scene-preview-request.ts:89-97) also drops layers, so the new right-hand preview cannot show the toggle either.
```
**Kopien:**
- apps/web/components/overlay-scene-canvas.tsx:314 (honours scene.layers order and enabled)
- packages/core/src/index.ts:1698-1720 (buildOverlaySceneDefinition computes enabled per layer)
- packages/core/src/overlay-layout.ts:1221-1239 and 1270-1272 (renderer draws clock and next card unconditionally)
- packages/core/src/index.ts:1826-1884 (drawtext fallback buildOverlayTextLinesFromScenePayload does honour ticker/queue)
- apps/web/lib/overlay-publish-review.ts:54-81 (reports clock/next/queue/schedule/ticker changes as going live)

## [U9] HIGH / drift — Scene preset cards and the insert/standby/reconnect preset selects change nothing on air - the renderer never reads resolvedPresetId
**Ort:** apps/web/components/overlay-settings-form.tsx:640

**Szenario:** Operator picks "Standby board" as the standby preset and "Bumper board" for inserts, sees a centred board in the studio rebuild, reviews ("Standby preset: replay-lower-third -> standby-board") and publishes. On air a standby slate and a regular asset get the identical lower-third + next card + clock layout; the only difference is the "STANDBY" label and headline text, which come from queueKind, not from the preset. All six cards are a choice with no on-air consequence.

**Beleg:**
```
Form offers OVERLAY_SCENE_PRESETS as cards (overlay-settings-form.tsx:639-651) and three selects (653-696). Core resolves the preset per queue kind into scene.resolvedPresetId (packages/core/src/index.ts:1688-1692, 1711). The on-air layout references `resolvedPresetId` only in the type (packages/core/src/overlay-layout.ts:211) and buildOverlaySceneLayout (1161-1310) branches only on `payload.scene.panelAnchor`, `surfaceStyle`, `titleScale`, `typographyPreset`. The studio rebuild does branch on it: overlay-scene-canvas.tsx:42-54 `effectiveScenePreset === "standby-board" ? "overlay-frame-standby" : ... "split-now-next" ? "overlay-frame-split" ...`. The drawtext fallback branches on it too (index.ts:1835-1874), so the preset exists in two renderers and is absent from the one that goes on air.
```
**Kopien:**
- apps/web/components/overlay-scene-canvas.tsx:41-54 (preset -> CSS frame class)
- packages/core/src/index.ts:1835-1874 (drawtext fallback per preset)
- packages/core/src/index.ts:1633-1651 (resolveOverlayScenePresetForQueueKind)
- apps/web/lib/overlay-publish-review.ts:39-42 (lists preset changes as going live)
- apps/web/app/(admin)/overlay-studio/page.tsx:72,95 ("Live scene preset" / "Draft preset" summary)

## [U10] HIGH / drift — Follow/sub/cheer/channel-point alerts and "Alert position" are promised as on-air but the renderer has no alert panel at all
**Ort:** apps/web/app/(admin)/overlays/page.tsx:61

**Szenario:** Alerts enabled, feature switch on: the Engagement tab shows "Follow/sub alerts - Runtime enabled. EventSub notifications will render as timed alerts." A viewer follows or subscribes to jimpanse247; nothing appears in the broadcast, because the render request has no alert input and the layout builds no alert node. The alert only ever draws on the un-captured /overlay browser page. (Secondary: the form's note "one Twitch reconnect ... so the broadcaster token includes the new scopes" and the identity flow requesting bits:read/channel:read:redemptions assume the connected identity is the broadcaster; with a moderator identity those scopes are granted on 3jakec's account, not jimpanse247's.)

**Beleg:**
```
overlays/page.tsx:58-79 prints "Runtime enabled. EventSub notifications will render as timed alerts." / "Cheer EventSub notifications will render as timed alerts." / "Redemption EventSub notifications will render as timed alerts..." gated only by isEngagement*RuntimeEnabled(settings, env, managedConfig). engagement-settings-form.tsx:144 "Follow, subscription, cheer, and channel-point alerts all render through the same internal overlay path." and 162-171 `<select ... value={alertPosition}>`. On-air request shape: packages/render/src/render.ts:20-30 `payload, engagement?, game?, chat?, sourceFrame?, width, height` - no alerts; worker buildSceneRenderRequest apps/worker/src/index.ts:2083-2098 fills exactly those; `grep -n 'alert\|follow' packages/core/src/overlay-layout.ts` is empty. Only the browser copy draws them: engagement-overlay.tsx:90-102 `{engagement.settings.alertsRuntimeEnabled && latestAlert ? <aside className={classForPosition("engagement-alert", engagement.settings.alertPosition)} ...`. Scopes: packages/core/src/broadcast-channel.ts:62-68 TWITCH_IDENTITY_SCOPES includes "bits:read", "channel:read:redemptions"; form line 178.
```
**Kopien:**
- apps/web/components/engagement-overlay.tsx:29,44-53,90-102 (browser-only alert rendering)
- apps/web/components/engagement-settings-form.tsx:142-179 (alert toggles, position, scope note)
- apps/web/app/(admin)/overlays/page.tsx:58-79 ("will render as timed alerts")
- packages/core/src/overlay-layout.ts:1161-1310 (buildOverlaySceneLayout: no alert node)

## [U11] MEDIUM / drift — The chatter-participation game (solo/small-group/crowd) exists only in the browser overlay; the on-air game input is a different record
**Ort:** apps/web/app/(admin)/overlays/page.tsx:95

**Szenario:** Operator enables "Enable chatter-participation game", ten people chat; the Engagement tab reports "Crowd ... is live with 10 active chatters in the last 5 minutes" and the form says the widget stays off-air only if chat goes quiet. The broadcast shows nothing: the worker only writes engagement_game runtime rows, and the playout render loop reads the separate chat-game (snake/minesweeper/2048) runtime record that is placed by a Studio "Chat Game" layer. The prompt/options widget is drawn solely by the un-captured /overlay page.

**Beleg:**
```
overlays/page.tsx:94-95 `${engagement.game.title} is live with ${engagement.game.activeChatterCount} active chatters in the last ${engagement.game.windowMinutes} minutes.`; engagement-settings-form.tsx:182-224 (section, line 223 "If chat goes quiet or every mode is disabled, the game widget stays off-air."). Web computes it for the snapshot: apps/web/lib/server/state.ts:973-977 `const game = buildEngagementGameOverlayState({ settings, runtime: engagementGameRuntime, recentEvents })`. Browser draws it: engagement-overlay.tsx:30 `const showGame = engagement.game.runtimeEnabled && engagement.game.mode !== ""` and 59-75. Worker: index.ts:7792-7800 reconcileEngagementGame only `await updateEngagementGameRuntimeRecord(snapshot)`; on-air game view index.ts:2114-2126 `currentSceneGame = buildChatGameOverlayViewFromRuntimeRecord(await readChatGameRuntimeRecord())` - packages/db/src/index.ts:6161 (updateEngagementGameRuntimeRecord) vs 6664 (readChatGameRuntimeRecord) are different records; `grep -rn buildEngagementGameOverlayState apps/worker/src` is empty.
```
**Kopien:**
- apps/web/components/engagement-overlay.tsx:30,59-75 (browser-only game widget)
- apps/web/lib/server/state.ts:973-1014 (engagement game summary for the snapshot)
- apps/web/components/engagement-settings-form.tsx:182-224 (mode switches)
- apps/worker/src/index.ts:2114-2126 (on-air game comes from the chat-game runtime instead)

## [U12] MEDIUM / drift — Chat rail: three different message caps and mode/style settings that never reach air
**Ort:** apps/web/components/engagement-settings-form.tsx:127

**Szenario:** Operator sets "Max chat messages" to 12, "Chat mode" to quiet and "Style" to card. On air the panel shows at most 8 messages and ignores mode and style (the persisted row carries only position and maxMessages); the /overlay browser page shows 3 (quiet CSS hides the rest) in card style; the form and the server accept 12. Three surfaces, three answers for one setting.

**Beleg:**
```
Form: engagement-settings-form.tsx:127 `<input max={12} min={1} ... value={maxMessages} />`, 96-104 chat mode select, 115-124 style select. Server clamp: packages/core/src/index.ts:393 `maxMessages: clampInteger(value?.maxMessages, defaults.maxMessages, 1, 12)`. Worker row: apps/worker/src/index.ts:581-584 `enabled, position: settings.chatPosition, maxMessages: settings.maxMessages, messages` (no chatMode/style); chat-overlay.ts:53 `Math.min(12, ...)`. Renderer: packages/core/src/overlay-layout.ts:606 `export const OVERLAY_CHAT_PANEL_MAX_MESSAGES = 8;` and 651 `const limit = Math.min(Math.max(1, operatorLimit), OVERLAY_CHAT_PANEL_MAX_MESSAGES);`; OverlayChatView (127-134) has only position/maxMessages/messages. Browser: engagement-overlay.tsx:25 `.slice(0, engagement.settings.maxMessages)`, 78 `engagement-chat-${chatMode} engagement-chat-${style}`; globals.css:1768-1769 `.engagement-chat-quiet .engagement-chat-message:nth-last-child(n + 4) { display: none; }`.
```
**Kopien:**
- packages/core/src/index.ts:393 (cap 12)
- apps/worker/src/chat-overlay.ts:53 (cap 12)
- packages/core/src/overlay-layout.ts:606,651 (cap 8)
- apps/web/components/engagement-overlay.tsx:25,78 (browser cap and mode/style classes)
- apps/web/app/globals.css:1768-1775 (quiet/flood CSS)

## [U13] HIGH / drift — Production env template switches the on-air scene renderer off while the studio preview still draws it
**Ort:** .env.production.example:54

**Szenario:** Operator follows README.md:176 (`cp .env.production.example .env`) and starts the stack. In the playout, apps/worker/src/index.ts:2522-2524 `if (!shouldUseSceneRenderer()) return null;` fires on every frame with no log line and no incident (only the catch branch below raises 'fell back to text mode'), so ffmpeg never receives an overlay PNG on fd 3 and the channel airs the drawtext text-mode overlay. Meanwhile the studio preview in the web image renders the full satori scene — apps/web never reads SCENE_RENDERER_ENABLED (zero references) — and docker/smoke-test.sh asserts `"renderer":"ok"` on that same web image. Result: the GUI shows an overlay that never reaches viewers, silently — the exact shape the operator complained about. The `0` was committed 2026-04-23 (8c85f76) for a Chromium capture path that no longer exists (docker/worker.Dockerfile:28 'No browser: the on-air overlay is rendered natively'); README.md:241 still justifies it with 'when Chromium capture is unstable'. The operator's live .env does not set the key (so today's channel is on the code default `1`), but every fresh production install per the README gets `0`.

**Beleg:**
```
.env.production.example:54  SCENE_RENDERER_ENABLED=0
.env.example:34             SCENE_RENDERER_ENABLED=1
apps/worker/src/index.ts:2066  return (process.env.SCENE_RENDERER_ENABLED || "1") !== "0";
apps/worker/src/index.ts:2522  if (!shouldUseSceneRenderer()) {
                                 return null;
                               }
README.md:241  `SCENE_RENDERER_ENABLED`: set to `0` to keep production on the text overlay path when Chromium capture is unstable
```
**Kopien:**
- .env.example:34
- apps/worker/src/index.ts:2066
- README.md:241
- docs/deployment.md:367

## [U14] HIGH / drift — The relay programme URL defaults to an unauthenticated value that the shipped relay config refuses — in code, both env templates, the smoke, and the operator's live .env
**Ort:** apps/worker/src/ffmpeg-runtime.ts:98

**Szenario:** docker/mediamtx.yml:13-14 makes mediamtx ask http://web:3000/api/relay/auth about every publish and read; packages/core/src/relay-ingest.ts:183-184 and :191-192 deny a publish on `live/program` and any read unless the password equals the internal relay key. Yet the worker default (ffmpeg-runtime.ts:98, :102), the core constant (relay-ingest.ts:28), .env.example:20-21, .env.production.example:37-38 and scripts/fresh-compose-bootstrap-smoke.sh:48-49 all carry `rtmp://relay:1935/live/program` with no credential, and the operator's live .env has the same unkeyed lines (checked: no `user=internal` present). The day the operator takes the documented emergency rollback `STREAM247_UPLINK_INPUT_MODE=rtmp` (docs/deployment.md:346) with the env as shipped, the playout (index.ts:5754 → getRelayOutputTarget → getRelayPublishUrl) publishes without a password → 403 bad-internal-key → ffmpeg exits → restart loop; the uplink (index.ts:6564 getRelayInputUrl) is refused the same way → channel dark for the duration of the incident, until someone finds Settings → Operations → Relay access. docs/deployment.md:352 documents the keyed-URL requirement, but every default still encodes the value that cannot work, and the only CI stack with the relay on (fresh-compose) runs in hls mode where the URL is never exercised, so nothing catches it.

**Beleg:**
```
apps/worker/src/ffmpeg-runtime.ts:98   return env.STREAM247_RELAY_OUTPUT_URL || "rtmp://relay:1935/live/program";
packages/core/src/relay-ingest.ts:183  if (args.request.path === RELAY_PROGRAM_ROLLBACK_PATH) {
packages/core/src/relay-ingest.ts:184    return matches(args.internalKey) ? { allow: true, role: "internal" } : { allow: false, reason: "bad-internal-key" };
docker/mediamtx.yml:13  authMethod: http
docker/mediamtx.yml:14  authHTTPAddress: http://web:3000/api/relay/auth
.env.production.example:37  STREAM247_RELAY_OUTPUT_URL=rtmp://relay:1935/live/program
```
**Kopien:**
- apps/worker/src/ffmpeg-runtime.ts:102
- packages/core/src/relay-ingest.ts:28
- .env.example:20-21
- .env.production.example:37-38
- scripts/fresh-compose-bootstrap-smoke.sh:48-49

## [U15] HIGH / drift — Every smoke that plays content runs the direct-output topology; production runs playout → HLS feed → uplink, which no CI stack ever exercises
**Ort:** scripts/runtime-parity-smoke.sh:303

**Szenario:** e2e-smoke.sh:41, queue-continuity-smoke.sh:179, runtime-parity-smoke.sh:303 and dev-stack.sh:62 all set STREAM_OUTPUT_URL to a file path and leave STREAM247_RELAY_ENABLED unset, so apps/worker/src/index.ts:5752-5754 makes the playout write the .flv itself and the uplink container idles (index.ts:6755-6756 stopAllUplinkProcesses("relay-disabled")). Production (.env.production.example:36,39; docs/deployment.md:346 'Production Compose enables the program-feed/uplink split by default') runs playout → HLS program feed on disk → uplink ffmpeg (index.ts:6562-6564) → Twitch. The one CI stack with the relay on (fresh-compose-bootstrap-smoke.sh:47) has no media and no destination and asserts only `ps --status running` (:96) and HTTP 200 on readiness (:117). So buildUplinkFfmpegCommand, the program-feed writer (buildProgramFeedOutputTarget), MEDIA-SEQUENCE continuity, the uplink stall/no-progress watchdogs and `healthcheck uplink` (index.ts:8125) never execute in CI. CHANGELOG.md:684-692 records what that costs: the channel was off the air for two and a half days with every liveness check green because the feed→uplink leg silently wrote nothing — exactly the leg no smoke runs. The script named 'runtime parity' asserts `primary.flv` written by the playout (:18, :407), a write production's playout never performs.

**Beleg:**
```
scripts/runtime-parity-smoke.sh:303  STREAM_OUTPUT_URL=/tmp/stream-output/primary   (no STREAM247_RELAY_ENABLED in the heredoc)
apps/worker/src/index.ts:5752  const playoutTargets = STREAM247_RELAY_ENABLED ? [relayTarget] : activeDestinationGroup.targets;
apps/worker/src/index.ts:5754  const outputTarget = STREAM247_RELAY_ENABLED ? getRelayOutputTarget() : buildFfmpegOutputTarget(activeDestinationGroup.targets);
apps/worker/src/index.ts:6564  const inputUrl = inputMode === "hls" ? getProgramFeedRuntimeConfig().playlistPath : getRelayInputUrl(process.env);
scripts/fresh-compose-bootstrap-smoke.sh:117  wget -qO- "http://127.0.0.1:${PORT}/api/system/readiness" >/dev/null
```
**Kopien:**
- scripts/e2e-smoke.sh:41
- scripts/queue-continuity-smoke.sh:179
- scripts/dev-stack.sh:62
- scripts/fresh-compose-bootstrap-smoke.sh:47-49
- .env.production.example:36-39

## [U16] MEDIUM / drift — 'Current release' is written in four places and three of them say different, stale versions — the Portainer check and a fresh install both act on the wrong one
**Ort:** docker-compose.yml:39

**Szenario:** package.json:4 says 1.5.37; docker-compose.yml:39,75,110,131 default to v1.5.20; .env.production.example:24-26 pins v1.5.20; docs/deployment.md:172 says the baseline is v1.5.19. The pins were last bumped in 446763a ('release: prepare 1.5.20') and 17 releases have shipped since. (a) scripts/portainer-stack-check.sh:5 takes its expected digests from .env.production.example, so the verification docs/deployment.md:192 tells the operator to run against a correctly deployed v1.5.37 DT stack prints `FAIL web/worker/uplink/playout: ... expected digest <v1.5.20>` for every service — the release gate says the good deployment is wrong. (b) README.md:176 `cp .env.production.example .env` then `docker compose up -d` starts v1.5.20 web/worker/playout under the v1.5.37 compose file, which now mounts docker/mediamtx.yml (docker-compose.yml:101) pointing relay auth at /api/relay/auth — a route that does not exist at v1.5.20 (`git ls-tree v1.5.20 apps/web/app/api` has no relay route), so every relay publish and read is refused — and the studio the operator opens lacks the renderer preview, push sources and relay-access panel the README describes.

**Beleg:**
```
docker-compose.yml:39   image: ${STREAM247_WEB_IMAGE:-ghcr.io/drjakeberg/stream247-web:v1.5.20}
.env.production.example:24  STREAM247_WEB_IMAGE=ghcr.io/drjakeberg/stream247-web:v1.5.20
docs/deployment.md:172  `.env.production.example` pins the current baseline (`v1.5.19`) for stable deployment.
package.json:4  "version": "1.5.37",
scripts/portainer-stack-check.sh:5  DEFAULT_ENV_FILE="$ROOT_DIR/.env.production.example"
```
**Kopien:**
- docker-compose.yml:75
- docker-compose.yml:110
- docker-compose.yml:131
- .env.production.example:24-26
- docs/deployment.md:172
- package.json:4

## [U17] MEDIUM / drift — CI runs a hand-copied subset of `pnpm validate`; the Release workflow runs the full one, so a green main can produce a tag with no images
**Ort:** .github/workflows/ci.yml:43

**Szenario:** ci.yml:43-46 runs `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build` as four separate steps. package.json:24 defines `validate` as those four PLUS `lint:css-tokens` (scripts/css-token-lint.sh:60 exits 1 when the raw-colour count exceeds the baseline of 78). release.yml:26 runs `pnpm validate`. scripts/release-preflight.sh:253-256 is a third copy, skipped in CI via RELEASE_PREFLIGHT_SKIP_VALIDATE=1 (ci.yml:53). A PR that adds one raw `#hex` to apps/web/app/**/*.css passes CI, merges, and `main-<sha>` images are published; the operator tags v1.5.38 → the Release job fails at `pnpm validate` before any release tag is pushed → a git tag and CHANGELOG entry with no images, the same outcome CHANGELOG.md:985 records for 1.5.18 ('unreleased; no images published').

**Beleg:**
```
.github/workflows/ci.yml:43-46
      - run: pnpm lint
      - run: pnpm typecheck
      - run: pnpm test
      - run: pnpm build
package.json:24  "validate": "pnpm lint && pnpm lint:css-tokens && pnpm typecheck && pnpm test && pnpm build",
.github/workflows/release.yml:26  - run: pnpm validate
```
**Kopien:**
- package.json:24
- .github/workflows/release.yml:26
- scripts/release-preflight.sh:253-256

## [U18] MEDIUM / drift — The 24h soak gate counts container restarts for web/worker/playout only — never uplink (the process that actually pushes to Twitch) or relay
**Ort:** scripts/soak-monitor.sh:132

**Szenario:** soak-monitor.sh:97-104 and :132 iterate `web worker playout`; docker-compose.yml:130 also runs `uplink`, which in the production topology is the only ffmpeg with a Twitch output, plus `relay`. If the uplink container exits and Docker restarts it faster than the 60s heartbeat staleness window (apps/web/lib/server/readiness.ts:7 RUNTIME_HEARTBEAT_STALE_MS = 60_000), each restart's first cycle rewrites `uplinkStatus: "running"` with a fresh heartbeat (index.ts:6614, :7006), readiness reports uplink ok, `uplink.unplannedRestartCount` only counts in-process ffmpeg watchdog restarts, and the container-restart check never inspects the uplink container → the soak logs `containerRestarts=web=0 worker=0 playout=0` for 24 hours and passes, and release-preflight.sh:263 presents that pass as the go signal to tag. Every other script keeps its own copy of the service list and none agree: dev-stack.sh:44 `worker playout uplink relay`, fresh-compose-bootstrap-smoke.sh:88-96 `worker relay playout uplink`, portainer-stack-check.sh:150 `web worker uplink playout relay`.

**Beleg:**
```
scripts/soak-monitor.sh:98   printf "web=%s worker=%s playout=%s" \
scripts/soak-monitor.sh:132  for service in web worker playout; do
docker-compose.yml:130  uplink:
apps/web/lib/server/readiness.ts:7  const RUNTIME_HEARTBEAT_STALE_MS = 60_000;
```
**Kopien:**
- scripts/dev-stack.sh:44
- scripts/fresh-compose-bootstrap-smoke.sh:88-96
- scripts/portainer-stack-check.sh:150
- docker-compose.yml:130

## [U19] HIGH / drift — On-air renderer ignores scene.layers, showClock, showNextItem, ticker and the scene preset that every GUI surface reports
**Ort:** packages/core/src/overlay-layout.ts:206

**Szenario:** Operator unchecks "Show next item" and "Clock" in the overlay form and publishes. The publish review says "Next item: shown -> hidden, Clock: shown -> hidden" (apps/web/lib/overlay-publish-review.ts:54-55), the control room prints "Next visible no · Clock no" and lists the enabled layers (apps/web/components/broadcast-control-room.tsx:303,311), and the /overlay browser page drops both panels (overlay-scene-canvas.tsx:314 iterates scene.layers). The PNG piped to ffmpeg keeps drawing the clock chip (overlay-layout.ts:1221-1238, unconditional) and the "NEXT · ... / Schedule not available" card, because buildNextCard only skips when nextTitle is empty and packages/core/src/index.ts:1812 sets nextTitle to `nextTitle || "Schedule not available"`, so it never is. Ticker text typed into the form never reaches air (tickerText is declared at :226 and never read), and choosing "Standby Board" vs "Minimal Chip" yields an identical frame (resolvedPresetId declared at :211, never read). The drawtext fallback on the same worker (index.ts:1826-1884) DOES branch on the preset and emit the ticker line, so the two on-air paths disagree with each other as well.

**Beleg:**
```
export type OverlayScenePayloadView = {
  scene: {
    surfaceStyle: string;
    panelAnchor: string;
    titleScale: string;
    typographyPreset: string;
    resolvedPresetId: string;
    customLayers?: OverlayCustomLayerView[];
  };   // no `layers` field at all
...
  const clockChip = label(clock, { ... });          // :1221, always built
  const topBar = [ row(..., [ banner ? buildBanner(...) : {...}, clockChip ]) ];   // :1232-1239
  if (votePanel) { rail.push(votePanel); } else if (nextCard) { rail.push(nextCard); }   // :1268-1272
// index.ts:1812
    nextTitle: nextTitle || "Schedule not available",
```
**Kopien:**
- packages/core/src/index.ts:1698-1720 (buildOverlaySceneDefinition computes layers[].enabled from showClock/showNextItem/showQueuePreview/showScheduleTeaser/disabledLayers/layerOrder)
- packages/core/src/index.ts:1826-1884 (buildOverlayTextLinesFromScenePayload, the on-air drawtext fallback, honours resolvedPresetId and tickerText)
- apps/web/components/overlay-scene-canvas.tsx:314 (browser /overlay page renders scene.layers.filter(enabled) incl. ticker)
- apps/web/components/broadcast-control-room.tsx:303,311
- apps/web/lib/overlay-publish-review.ts:54-55
- apps/web/components/overlay-settings-form.tsx:1310,1314
- apps/worker/src/index.ts:2727-2728 (worker feeds showClock/showNextItem into the payload that only reaches scene.layers)

## [U20] HIGH / silent — Alerts EventSub registers on the connected identity's own channel; core gives the broadcaster slot no alert scopes and no gate models the split
**Ort:** packages/core/src/broadcast-channel.ts:52

**Szenario:** Operator's real setup: identity = moderator 3jakec, configured broadcast channel = jimpanse247, alerts enabled. apps/web/lib/server/twitch.ts:185 stores `broadcasterId = twitchUser.id` (3jakec's id) on the identity row; apps/worker/src/twitch-eventsub.ts:318 reads `args.state.twitch.broadcasterId` and registers channel.follow/subscribe/cheer/channel_points with `broadcaster_user_id: broadcasterId` (:44-75), never consulting the broadcaster slot or resolveBroadcastChannelLogin (no reference in that file). Result: a follow, sub or cheer on jimpanse247 never produces an on-air alert, while a follow on 3jakec's empty channel does. Unlike title/category, which visibly "waits for broadcaster" via resolveTwitchMetadataSyncGate (:138-157), nothing reports this — and even a gate could not fix it, because the broadcaster slot as defined in core requests only channel:manage:broadcast + channel:manage:schedule, while bits:read / channel:read:redemptions / channel:read:subscriptions sit in TWITCH_IDENTITY_SCOPES (:62-69), where Twitch grants them only for the moderator's own channel.

**Beleg:**
```
// Exactly the writes the broadcaster slot exists for — title/category and schedule. Asking for
// more would turn a narrowly scoped metadata connection into a second fully privileged account
// for no benefit; the identity connection keeps every other scope.
export const TWITCH_BROADCASTER_SLOT_SCOPES = ["channel:manage:broadcast", "channel:manage:schedule"];
...
export const TWITCH_IDENTITY_SCOPES = [
  "channel:manage:broadcast",
  "channel:manage:schedule",
  "bits:read",
  "channel:read:redemptions",
  "moderator:manage:chat_settings",
  "moderator:read:followers",
  "channel:read:subscriptions",
// apps/worker/src/twitch-eventsub.ts:318
  const broadcasterId = args.state.twitch.broadcasterId.trim();
// apps/web/lib/server/twitch.ts:185
  const broadcasterId = twitchUser.id;
```

## [U21] HIGH / drift — findScheduleConflicts wraps a past-midnight block onto the same weekday while buildScheduleOccurrences carries it into the next day
**Ort:** packages/core/src/index.ts:2853

**Szenario:** Case A: existing block Monday 23:00 for 120 min; operator adds Monday 00:30 for 30 min. findScheduleConflicts splits the first into [1380,1440] and [0,60] on Monday and compares only same-dayOfWeek blocks (:2866), so [0,60] overlaps [30,60] and the API throws "Schedule blocks overlap" (apps/web/app/api/schedule/blocks/route.ts:143,275,390) — although per toScheduleOccurrence the 00:00-01:00 part belongs to Tuesday and the two blocks never share a minute. Case B: Monday 23:00 for 120 min plus Tuesday 00:00 for 60 min is accepted (different dayOfWeek -> skipped) and shows no conflict marker on the schedule page (:63); at Tuesday 00:30 buildScheduleOccurrences yields both, findCurrentScheduleOccurrence picks the most recently started (:3054-3056), and the Monday-night block silently loses its last hour on air.

**Beleg:**
```
    const currentRanges =
      current.startMinuteOfDay + current.durationMinutes <= 24 * 60
        ? [[current.startMinuteOfDay, current.startMinuteOfDay + current.durationMinutes]]
        : [
            [current.startMinuteOfDay, 24 * 60],
            [0, (current.startMinuteOfDay + current.durationMinutes) % (24 * 60)]
          ];
    ...
      if (candidate.dayOfWeek !== current.dayOfWeek) {
        continue;
      }
// vs :3003-3008
  const carriedOver = args.blocks
    .filter((block) => block.dayOfWeek === previousDayOfWeek && block.startMinuteOfDay + block.durationMinutes > MINUTES_PER_DAY)
    .map((block) => toScheduleOccurrence({ block, date: args.date, carriesOverFromPreviousDay: true }));
```
**Kopien:**
- packages/core/src/index.ts:2930-2964 (toScheduleOccurrence: effectiveStartMinuteOfDay = start - 1440 for carry-over)
- packages/core/src/index.ts:2992-3011 (buildScheduleOccurrences: overflow lands on the next date)
- packages/core/src/index.ts:2783-2793 (isCurrentScheduleTime: string-compare wrap onto the same day, still called from apps/worker/src/cuepoints.ts:51 and apps/web/lib/server/state.ts:1230)
- packages/core/src/index.ts:2919 (summarizeScheduleWeek lastEndMinute wraps modulo onto the same day)

## [U22] MEDIUM / drift — On-air "Next" title is computed from a pool-eligibility rule that differs from the one the playout actually follows
**Ort:** packages/core/src/index.ts:2316

**Szenario:** Pool has insertAssetId = bumper and insertEveryItems = 0 (apps/web/app/api/pools/route.ts:21 stores this pair; the GUI only shows "Insert every N" when N > 0). Sorted eligible order A, bumper, B; cursor on A. The worker's selectPoolAsset uses getPoolEligibleAssets (apps/worker/src/index.ts:3929: excludes the insert asset only when insertEveryItems > 0), so the bumper plays next; the overlay's next title goes through core lookaheadVideoTitleFromPool (worker :3961-3975, :2705-2707) -> getSchedulePreviewEligibleAssets, which drops the insert asset unconditionally, so the on-air Next card says "Next: B" while the bumper plays. Second reachable case: a Twitch VOD whose cache download just failed is skipped by the worker (isAssetBlockedForAutomaticSelection, :3943, :779-781) but named by the core lookahead, so the Next card promises a video that is skipped. The web schedule page and program-week-lens use the same core lookahead (schedule/page.tsx:96, program-week-lens.tsx:34) and the control room uses a fourth copy (state.ts:739), so the GUI agrees with the wrong answer.

**Beleg:**
```
// core :2315-2318 (preview / lookahead)
  const excludedAssetIds = new Set<string>();
  if (pool.insertAssetId) {
    excludedAssetIds.add(pool.insertAssetId);
  }
// core :2536 (materialized week)
  if (args.pool?.insertAssetId && Math.max(args.pool?.insertEveryItems ?? 0, 0) > 0) {
// apps/worker/src/index.ts:3929-3931, :3943 (what actually plays)
  if (pool.insertAssetId && pool.insertEveryItems > 0) {
    excludedAssetIds.add(pool.insertAssetId);
  }
  ...
        isAssetBlockedForAutomaticSelection(asset) ||
```
**Kopien:**
- packages/core/src/index.ts:2536-2553 (materializePoolWindow eligibility)
- apps/worker/src/index.ts:3924-3959 (getPoolEligibleAssets, the rule the playout obeys)
- apps/worker/src/index.ts:3961-3975 (overlay next title via core lookaheadVideoTitleFromPool)
- apps/web/lib/server/state.ts:739 (getScheduleOccurrenceLookaheadTitle via buildSchedulePreviewVideoSlots)
- packages/core/src/index.ts:2293-2305, packages/core/src/index.ts:2517-2527, apps/worker/src/index.ts:3951-3958 (three identical sort comparators)
- packages/core/src/index.ts:2276-2291, packages/core/src/index.ts:2503-2515 (two identical duration estimators)

## [U23] MEDIUM / drift — Core accepts and advertises local (leading-slash) logo/image URLs that the on-air renderer silently drops
**Ort:** packages/core/src/index.ts:1297

**Szenario:** Operator adds a Logo layer with URL `/api/assets/<id>/thumbnail` (a path the app itself serves, apps/web/components/asset-library-browser.tsx:677). sanitizeOverlaySceneUrl keeps it, the layer-kind description promises "safe remote/local URLs" (:1112-1115), the /overlay browser page and the studio rebuild draw it (overlay-scene-canvas.tsx:154-159 uses layer.url as-is). On air, buildMediaPanel returns null for anything that is not data:image/ or http(s) (overlay-layout.ts:1021-1024) and the worker only inlines data URIs for source layers (apps/worker/src/index.ts:2194-2200, :2441), so the logo is absent from the broadcast with no error and no status. The studio comment at overlay-settings-form.tsx:559-563 already names this gap; the form, the sanitizer and the description still accept it.

**Beleg:**
```
// index.ts:1297-1299
  if (trimmed.startsWith("/")) {
    return trimmed;
  }
// index.ts:1112-1115
    id: "logo",
    label: "Logo Layer",
    description: "Contained brand mark or channel badge with safe remote/local URLs.",
// overlay-layout.ts:1021-1024
  const url = String(placement.url ?? "");
  if (!/^(data:image\/|https?:\/\/)/.test(url)) {
    return null;
  }
```
**Kopien:**
- packages/core/src/overlay-layout.ts:1021-1024 (on-air rule: data:image/ or http(s) only)
- apps/web/components/overlay-scene-canvas.tsx:154-159 (browser page draws layer.url verbatim)
- apps/web/components/overlay-settings-form.tsx:559-563 (studio note acknowledging the gap)

## [U24] LOW / drift — Custom-layer placement box is clamped three ways with two different safe areas (3.75%/5.19% on air vs 5%/5% on the /overlay page)
**Ort:** packages/core/src/overlay-layout.ts:717

**Szenario:** Text layer at xPercent 0, yPercent 0, safe area on. On the /overlay browser page (apps/web/app/overlay/page.tsx -> LiveOverlay -> OverlaySceneCanvas -> resolveOverlayCustomLayerStyle) the box starts at 5%/5% of the frame = 96px/54px at 1920x1080 (globals.css:29-36); the satori frame piped to ffmpeg starts it at px(72)/px(56) = 72px/56px. The same layer is 24px further left and 2px lower on air than on the page an operator opens to check it. The three clamp copies also disagree on minima (core index.ts x/y max 90, width min 10, opacity min 5; core renderer x/y max 100, width min 10, opacity min 5; web width/height min 0, opacity min 0) — the stored-value normalizer masks most of that today, but only because it happens to run first.

**Beleg:**
```
// overlay-layout.ts:717-725
  const safeX = placement.allowOutsideSafeArea ? 0 : px(72);
  const safeY = placement.allowOutsideSafeArea ? 0 : px(56);
  ...
  const xPercent = clampPlacementPercent(placement.xPercent, 0, 100);
  const widthPercent = clampPlacementPercent(placement.widthPercent, 10, 100 - xPercent);
// index.ts:1564-1568
      xPercent: clampOverlaySceneNumber(raw.xPercent, 0, 90, raw.kind === "text" ? 4 : 62),
      widthPercent: clampOverlaySceneNumber(raw.widthPercent, 10, 100, raw.kind === "text" ? 34 : 26),
      opacityPercent: clampOverlaySceneNumber(raw.opacityPercent, 5, 100, 100),
// apps/web/lib/overlay-layout.ts:39-42,50
  const xPercent = clampPercent(layer.xPercent, 0, 100);
  const widthPercent = clampPercent(layer.widthPercent, 0, 100 - xPercent);
      opacity: clampPercent(layer.opacityPercent, 0, 100) / 100
// apps/web/app/globals.css:29-36
  --safe-area-top-percent: 5%;  --safe-area-left-percent: 5%;
  --overlay-safe-area-width-percent: 90%;  --overlay-safe-area-height-percent: 90%;
```
**Kopien:**
- packages/core/src/index.ts:1564-1568 (normalizeOverlaySceneCustomLayers clamp)
- apps/web/lib/overlay-layout.ts:39-42,50,59 (resolveOverlayCustomLayerStyle clamp, used by the /overlay page and the studio rebuild)
- apps/web/app/globals.css:29-36 (browser safe area 5%/5%/90%/90%)
- apps/web/components/overlay-settings-form.tsx:559-563 (studio note acknowledging the safe-area gap)

## [U25] HIGH / drift — Custom-layer placement, safe area and clamps still exist as three copies; /overlay page and studio text still present the HTML copy as the captured output
**Ort:** apps/web/lib/overlay-layout.ts:39

**Szenario:** Default Logo layer (x 76, y 8, w 16, h 12 from apps/web/lib/overlay-studio-defaults.ts:42-56) at 1920x1080: the renderer (authoritative) places it at left 1422 / top 133 / 284x116 px because its safe margins are px(72) x px(56) = 3.75% x 5.19%; the studio 'rebuild' and the /overlay page place it at left 1409 / top 132 / 276x117 because globals.css declares a 5% margin and a 90% safe box. For an unsaved draft the studio inputs accept width 3 / height 2 / opacity 2 while typing (form clamps only on blur): the rebuild draws a 3%-wide, near-invisible box; on air it is 10% x 8% at 5% opacity (core clamps at overlay-layout.ts:724-725, :919/:985/:1041/:1115). The operator is also told at overlay-studio/page.tsx:37 that 'Stream247 captures <APP_URL>/overlay as its internal overlay output' and given an 'Open overlay page' link at :121 — but no worker code calls getSceneRendererBaseUrl (apps/worker/src/on-air-scene.ts:48) any more; the worker rasterises packages/core/src/overlay-layout.ts directly (apps/worker/src/scene-renderer.ts:1-12). Anyone who points OBS or a check at /overlay sees the HTML imitation, never the frame that goes out.

**Beleg:**
```
apps/web/lib/overlay-layout.ts:39-47: `const widthPercent = clampPercent(layer.widthPercent, 0, 100 - xPercent); const heightPercent = clampPercent(layer.heightPercent, 0, 100 - yPercent); ... opacity: clampPercent(layer.opacityPercent, 0, 100) / 100` with `left: calc(var(--safe-area-left-percent) + ...)`.  apps/web/app/globals.css:29-36: `--safe-area-top-percent: 5%; --safe-area-left-percent: 5%; --overlay-safe-area-width-percent: 90%; --overlay-safe-area-height-percent: 90%`.  packages/core/src/overlay-layout.ts:717-725: `const safeX = placement.allowOutsideSafeArea ? 0 : px(72); const safeY = ... px(56); ... clampPlacementPercent(placement.widthPercent, 10, 100 - xPercent); clampPlacementPercent(placement.heightPercent, 8, 100 - yPercent)`.  packages/core/src/overlay-layout.ts:699 comment: `/** Clamps a custom-layer percent box the same way the studio preview does. */` (it does not).  apps/web/app/(admin)/overlay-studio/page.tsx:37: `Stream247 captures <code>{getAbsoluteAppUrl(state, "/overlay")}</code> as its internal overlay output.`
```
**Kopien:**
- packages/core/src/overlay-layout.ts:700-733 (authoritative: resolvePlacementBox, what the worker rasterises)
- apps/web/lib/overlay-layout.ts:5-7,39-61 (HTML copy: min width/height 0, opacity 0..100, CSS-var safe area)
- apps/web/app/globals.css:25-36 (HTML copy's safe area: 5% margins, 90% box)
- tests/unit/live-overlay-safe-area.test.ts:17-45 (pins the HTML copy, not the renderer)
- apps/web/components/overlay-scene-canvas.tsx:15,160 (studio 'rebuild' consumer)
- apps/web/components/live-overlay.tsx:4,28-31 and apps/web/app/overlay/page.tsx:15-22 (the /overlay page, still served)
- apps/web/app/(admin)/overlay-studio/page.tsx:37,121 (tells the operator /overlay is what is captured)
- apps/worker/src/on-air-scene.ts:48 getSceneRendererBaseUrl (dead: zero callers in apps/worker/src)

## [U26] HIGH / drift — Overlay scale factor is computed by three different formulas; at the default 720p profile the GUI copy is 1.5x the on-air scale
**Ort:** apps/web/lib/overlay-layout.ts:10

**Szenario:** Output profile left at the default 720p30 (packages/core/src/index.ts:205-210, width 1280 / height 720): the renderer scales the whole design grid by 1280/1920 = 0.667; the studio rebuild and the /overlay page scale by min(1, max(0.62, 720/720)) = 1.0. Every gap, padding, chip and panel in the imitation is therefore 1.5x larger relative to the frame than what viewers see. At 360p30 (640x360) it is 0.35 vs 0.62 = 1.77x. The two only agree at 1080p. The worker's drawtext fallback (apps/worker/src/index.ts:1770-1777, used when overlayMode is 'text', index.ts:5104) uses the same 0.62/720 formula as the web copy, so the fallback text and the scene PNG are sized by different rules on the same channel.

**Beleg:**
```
packages/core/src/overlay-layout.ts:41-42: `export function overlayScale(width: number): number { return Math.max(0.35, width / 1920); }`  apps/web/lib/overlay-layout.ts:10: `const overlayScale = Math.min(1, Math.max(0.62, outputHeight / 720));`  apps/worker/src/output-settings.ts:29-31: `export function getOutputScaleFactor(output: StreamOutputSettings): number { return Math.min(1, Math.max(0.62, output.height / 720)); }`  apps/worker/src/index.ts:1771: `const scale = getOutputScaleFactor(output);`
```
**Kopien:**
- packages/core/src/overlay-layout.ts:41-42 (authoritative for the scene PNG)
- apps/web/lib/overlay-layout.ts:10 (studio rebuild + /overlay page)
- apps/worker/src/output-settings.ts:29-31 (drawtext text-fallback, apps/worker/src/index.ts:1770-1777 and :2027)

## [U27] HIGH / drift — The live scene payload is derived twice (web buildActiveScenePayload vs worker writeOnAirOverlay) with different next-block lookup, different time-label format, different fallbacks and chapter-aware titles only on air
**Ort:** apps/web/lib/server/state.ts:1062

**Szenario:** Schedule whose last block ends 23:00 and first block starts 06:00. At 23:30 the web copy (getNextScheduleItem, state.ts:369-380, findNextScheduleOccurrenceAcrossDays with a 7-day lookahead) feeds 'Morning Replay · 06:00 to 07:00' into the studio live payload, the /overlay page, the public /channel snapshot (state.ts:1308) and the dashboard 'Next block' card (dashboard/page.tsx:64); the worker copy (index.ts:3901-3919, same-day findNextScheduleOccurrence) finds nothing, so the on-air lower third reads 'Coming up next' / 'No next block configured'. Even when both find the same block (10:00 with a 11:00-12:00 block) the on-air label is '11:00-12:00' while every GUI surface says '11:00 to 12:00'. During a chaptered asset the worker puts the chapter title on air (resolveOnAirChapterTitle, index.ts:2801-2820); the web copy only knows buildAssetDisplayTitle(currentAsset) (state.ts:1076), so the studio/overlay/public title differs from the broadcast for the length of every chapter.

**Beleg:**
```
apps/web/lib/server/state.ts:376: `return findNextScheduleOccurrenceAcrossDays({ blocks: state.scheduleBlocks, date: scheduleMoment.date, currentTime: scheduleMoment.time });`  apps/worker/src/index.ts:3916: `return findNextScheduleOccurrence({ occurrences, currentTime: scheduleMoment.time, currentOccurrence: current });`  state.ts:1106: `nextTimeLabel: nextScheduleItem ? `${nextScheduleItem.startTime} to ${nextScheduleItem.endTime}` : "No next block configured"`  index.ts:2854: `nextTimeLabel: overrides.nextTimeLabel || (nextItem ? `${nextItem.startTime}-${nextItem.endTime}` : "No next block configured")`  state.ts:1105 fallback `"Schedule not available"` vs index.ts:2852 `nextTitle: overrides.nextTitle || nextItem?.title || "Coming up next"` and index.ts:2778 `"Programming will resume shortly"`.  index.ts:2845-2851: `currentTitle: overrides.currentTitle || resolveOnAirChapterTitle(state, asset) || buildAssetDisplayTitle(asset) || ...` vs state.ts:1076 `const currentAssetTitle = buildAssetDisplayTitle(currentAsset);`
```
**Kopien:**
- apps/worker/src/index.ts:2820-2856 writeOnAirOverlay (authoritative: feeds the on-air renderer and drawtext)
- apps/worker/src/index.ts:2692-2748 buildWorkerScenePayload and :2774-2784 writeStandbySlate
- apps/worker/src/index.ts:3884-3919 worker getCurrentScheduleItem/getNextScheduleItem (same-day, packages/core/src/index.ts:3059)
- apps/web/lib/server/state.ts:354-380 web getCurrentScheduleItem/getNextScheduleItem (7-day lookahead, packages/core/src/index.ts:3074)
- apps/web/lib/server/state.ts:1062-1110 buildActiveScenePayload (studio live view, /overlay, public snapshot at :1308)
- apps/web/app/(admin)/dashboard/page.tsx:63-64 and apps/web/app/(admin)/overlay-studio/page.tsx (Current/Next block cards from the web copy)
- apps/worker/src/index.ts:2801-2820 resolveOnAirChapterTitle (exists only on the worker side)

## [U28] MEDIUM / drift — Chat panel message cap: settings, form, worker projection and browser overlay allow 12, the on-air renderer silently caps at 8
**Ort:** packages/core/src/overlay-layout.ts:606

**Szenario:** Operator sets 'Max chat messages' to 12. The form accepts it (max=12), the API stores 12 (core clamp 1..12), the worker projection keeps 12 (chat-overlay.ts:53), and the /overlay browser page lists 12 rows (engagement-overlay.tsx:25). The frame that goes to ffmpeg shows the newest 8 (overlay-layout.ts:651, :659). No surface in the GUI states that the on-air ceiling is 8; the operator sees a 12-row overlay in the browser and a shorter one on Twitch, with no explanation.

**Beleg:**
```
packages/core/src/overlay-layout.ts:606: `export const OVERLAY_CHAT_PANEL_MAX_MESSAGES = 8;`  :650-651: `const operatorLimit = Number.isFinite(chat.maxMessages) ? Math.round(chat.maxMessages) : 1; const limit = Math.min(Math.max(1, operatorLimit), OVERLAY_CHAT_PANEL_MAX_MESSAGES);`  packages/core/src/index.ts:393: `maxMessages: clampInteger(value?.maxMessages, defaults.maxMessages, 1, 12),`  apps/web/components/engagement-settings-form.tsx:127: `<input max={12} min={1} ... value={maxMessages} />`  apps/worker/src/chat-overlay.ts:53: `const maxMessages = Math.min(12, Math.max(1, Math.round(source.maxMessages) || 1));`  apps/web/components/engagement-overlay.tsx:22-25: `engagement.recentEvents.filter((event) => event.kind === "chat").slice(0, engagement.settings.maxMessages)`
```
**Kopien:**
- packages/core/src/overlay-layout.ts:606,650-651,659 (authoritative: what is rasterised)
- packages/core/src/index.ts:393 (settings clamp 1..12)
- apps/web/components/engagement-settings-form.tsx:126-127 (form max 12)
- apps/worker/src/chat-overlay.ts:53 (worker projection cap 12)
- apps/web/components/engagement-overlay.tsx:22-26 (browser /overlay shows maxMessages rows)

## [U29] MEDIUM / drift — Dashboard Twitch card re-derives 'who is the broadcaster' from the identity record; on the moderator setup it names the moderator as Broadcaster while core and the worker name the configured channel
**Ort:** apps/web/components/twitch-connection-status.ts:45

**Szenario:** Operator's actual setup: identity account 3jakec (moderator) connected, broadcast channel jimpanse247 configured, broadcaster slot not connected. describeTwitchConnection(state.twitch) prints 'Connected' / 'Broadcaster 3jakec' with an empty consequence line (dashboard/page.tsx:82-84), because state.twitch.broadcasterLogin is the identity login (state.ts:1279 passes that very field as identityLogin). The same page computes the core gate (dashboard/page.tsx:30-34) and, lower down (:291-293), says title and category for jimpanse247 are waiting for the broadcaster slot; the worker raises incident twitch.metadata.waiting-for-broadcaster (index.ts:7369-7385) and writes nothing. The card's consequence text (PAUSED_WHILE_DISCONNECTED, :23-24) additionally re-derives 'metadata paused' from the identity status alone, which is not the rule core applies (resolveTwitchMetadataSyncGate looks at the broadcaster slot when the channel is split).

**Beleg:**
```
apps/web/components/twitch-connection-status.ts:42-47: `if (twitch.status === "connected") { return { label: "Connected", detail: `Broadcaster ${twitch.broadcasterLogin || twitch.broadcasterId}`, consequence: "" }; }`  apps/web/lib/server/state.ts:1277-1279: `broadcasterLogin: resolveBroadcastChannelLogin({ ... identityLogin: state.twitch.broadcasterLogin`  packages/core/src/broadcast-channel.ts:138-156: `export function resolveTwitchMetadataSyncGate(... if (!isBroadcastChannelSplit(args)) { return { mode: "identity" }; } ... connectionMatchesChannel ? { mode: "broadcaster", ... } : { mode: "waiting-for-broadcaster", ... }`  apps/web/app/(admin)/dashboard/page.tsx:35: `const twitchConnection = describeTwitchConnection(state.twitch);` and :83: `<p className="subtle">{twitchConnection.detail}</p>`
```
**Kopien:**
- packages/core/src/broadcast-channel.ts:27 resolveBroadcastChannelLogin and :138 resolveTwitchMetadataSyncGate (authoritative; used by the worker at apps/worker/src/index.ts:7346-7385)
- apps/web/lib/server/state.ts:1277-1279 (public snapshot resolves via core)
- apps/web/app/(admin)/dashboard/page.tsx:30-34 (gate computed via core) vs :35,82-84 (card from the identity record)
- apps/web/components/twitch-connection-status.ts:23-24,45,53,71 (label and consequence re-derived from state.twitch alone)

## [U30] LOW / drift — Worker heartbeat staleness threshold is declared independently in three files (plus a fourth, different value in the incident engine)
**Ort:** apps/worker/src/index.ts:350

**Szenario:** The worker's copy is documented as tied to the reconciliation cadence (index.ts:348-349: 'keep the stale window above the steady-state cadence'). Raising the cadence and that constant to e.g. 300 s leaves the two web copies at 240 s: the dashboard prints 'Worker heartbeat is stale. Reconciliation may be stuck.' (state.ts:1410-1413) and /api/ready reports worker 'degraded' (readiness.ts:60) while the worker container's own healthcheck (index.ts:8117) passes — a false alarm the operator cannot reconcile from any one screen. Already today the four copies disagree between 121 s and 240 s: incident-classes.ts:621 treats the worker area as unhealthy (worker incidents stop auto-resolving) while dashboard, readiness and the healthcheck all say OK.

**Beleg:**
```
apps/worker/src/index.ts:350: `const WORKER_HEARTBEAT_STALE_MS = 240_000;` and :8117: `if (now - new Date(lastWorkerCycle).getTime() > WORKER_HEARTBEAT_STALE_MS) { throw new Error("Worker heartbeat is stale."); }`  apps/web/lib/server/state.ts:1398: `const WORKER_HEARTBEAT_STALE_MS = 240_000;` :1410: `if (ageMs > WORKER_HEARTBEAT_STALE_MS) {`  apps/web/lib/server/readiness.ts:6: `const WORKER_HEARTBEAT_STALE_MS = 240_000;` :60: `workerHeartbeatAt > 0 ? (now - workerHeartbeatAt < WORKER_HEARTBEAT_STALE_MS ? "ok" : "degraded") : "not-ready"`  apps/worker/src/incident-classes.ts:500: `const WORKER_CYCLE_FRESH_MS = 120_000;` :621: `if (ageMs(input.workerHeartbeatAt, nowMs) <= WORKER_CYCLE_FRESH_MS) {`
```
**Kopien:**
- apps/worker/src/index.ts:350,8117 (container healthcheck; the one that restarts the process)
- apps/web/lib/server/state.ts:1398,1410 (dashboard worker health card)
- apps/web/lib/server/readiness.ts:6,60 (/api/ready, consumed by soak-monitor.sh)
- apps/worker/src/incident-classes.ts:500,621 (incident-area health, 120 s)

## [U31] CRITICAL / bug — Every natural asset boundary puts the next asset on air in drawtext text mode, not the satori scene
**Ort:** apps/worker/src/index.ts:5104

**Szenario:** Asset A reaches EOF (exit code 0). The exit handler (index.ts:5489-5499) writes status "idle", lastExitCode "0" (String(0) is truthy), heartbeatAt = now, then requests an immediate cycle. The next cycle calls startOrSwitchPlayout with runtimeStatus "idle", runtimeLastExitCode "0", runtimeHeartbeatAt < 60 s old. shouldSkipInitialSceneCapture (ffmpeg-runtime.ts:204) accepts "idle" as a recovery state and returns true -> initialSceneFrame is never rendered -> overlayMode becomes "text". Asset B's entire run (hours for a VOD) is the DejaVu drawtext box from getMediaOverlayFilter (index.ts:1771): no scene preset, no custom layers, no chat/game/engagement panels, no source panel, and a decided live PiP attach is silently dropped (attachLive requires scene mode, index.ts:1817). No incident is raised because prepareSceneRendererFrame is never called; only a scene.render.recovery.skip log line. Meanwhile the studio (apps/web/lib/server/scene-preview-renderer.ts) draws the satori scene. Scene mode returns only on a mid-asset switch or a watchdog/duration-bound stop (state read before the stop still says "running"), so on a channel whose assets end by EOF the on-air overlay is text mode almost permanently after the first process. The 60 s skip window was built for the 10 s Chromium capture; with the ~70 ms satori render it buys nothing and costs the whole overlay.

**Beleg:**
```
index.ts:5080-5104: const skipInitialSceneCapture = shouldSkipInitialSceneCapture({ overlayEnabled, switching, playoutStatus: args.runtimeStatus, lastExitCode: args.runtimeLastExitCode, heartbeatAt: args.runtimeHeartbeatAt, windowMs: 60_000 }); ... const initialSceneFrame = args.overlayEnabled && !skipInitialSceneCapture ? await prepareSceneRendererFrame(outputSettings) : null; const overlayMode = !args.overlayEnabled ? "none" : initialSceneFrame ? "scene" : "text";

ffmpeg-runtime.ts:204: if (args.playoutStatus !== "failed" && args.playoutStatus !== "idle" && args.playoutStatus !== "recovering") { return false; }

index.ts:5489-5499 (exit handler, natural boundary): status: nonFailureExit ? "idle" : ..., heartbeatAt: exitedAt, ... lastExitCode: String(code ?? signal ?? "")
```

## [U32] HIGH / drift — Standby/reconnect slate in scene mode keeps drawing the previous asset's overlay; the standby preset/headline never reach air
**Ort:** apps/worker/src/index.ts:2752

**Szenario:** Asset A is on air in scene mode. Its last ready sibling is disabled/removed (or the pool empties), so the cycle selects standby: writeStandbySlate(state, "standby") builds the standby payload but only writes text lines to /tmp/stream247-standby.txt; currentScenePayload is untouched (the sole assignment is in writeOnAirOverlay, index.ts:2866). isMatchingRunningSelection is false -> startOrSwitchPlayout with switching=true -> scene mode -> prepareSceneRendererFrame renders currentScenePayload, i.e. A's title with the "asset" preset, and the renderer loop keeps re-rendering that payload for as long as the slate is up. The scene-mode standby command (index.ts:2023) overlays only the fd-3 pipe and never reads the standby text file, so the operator's standbyScenePreset/standbyHeadline (and reconnectScenePreset/reconnectHeadline on the same path) are dead in scene mode. The studio builds its preview from queueItems[0].kind = "standby" (state.ts:1078) and shows the standby preset. A GUI 'refresh' on a standby process (index.ts pendingAction === "refresh" branch) also only rewrites the text file and changes nothing on air.

**Beleg:**
```
index.ts:2752-2786 writeStandbySlate(...): const payload = buildWorkerScenePayload({ state, queueKind, currentTitle: currentItem?.title || "Stand by", ... }); const lines = buildOverlayTextLinesFromScenePayload(payload); await fs.writeFile(standbySlatePath, ...);   // no `currentScenePayload = payload`

index.ts:2866 (writeOnAirOverlay, only assignment): currentScenePayload = payload;

index.ts:2023 (getStandbyFfmpegCommand, scene branch): command.push("-filter_complex", "[0:v][2:v]overlay=0:0:format=auto[vout]", "-map", "[vout]", "-map", "1:a");   // standbySlatePath only used in the text branch
```
**Kopien:**
- apps/worker/src/index.ts:2866
- apps/worker/src/index.ts:2023
- apps/web/lib/server/state.ts:1078

## [U33] MEDIUM / airtime — Late exit of a deadline-abandoned ffmpeg wipes the state of the process that replaced it and kills its overlay writer
**Ort:** apps/worker/src/index.ts:5412

**Szenario:** stopPlayoutProcess sends SIGTERM, SIGKILL at 5 s, and at 20 s (PLAYOUT_STOP_DEADLINE_MS, index.ts:4789-4794) gives up on a child stuck in uninterruptible I/O (e.g. media on a hung NFS mount) and resolves; startOrSwitchPlayout spawns ffmpeg #2 and starts its scene renderer loop. When the old child finally dies, its own child.on("exit") handler runs with no identity check: it sets playoutProcess = null, playoutAssetId = "", playoutProcessStartedAtMs = 0, calls stopSceneRendererLoop() (aborting #2's writer, which then ends #2's fd-3 pipe), and writes status idle/failed with processPid 0. From then on #2 is an orphan: isPlayoutProcessRunning() is false so the feed-stall and feed-audio watchdogs can never restart it, the duration bound never fires for it, and the next cycle sees no process and spawns ffmpeg #3 while #2 still appends to the same program.m3u8. finalize() in stopPlayoutProcess has exactly the guard this handler lacks (index.ts:4751).

**Beleg:**
```
index.ts:5379-5413 child.on("exit", (code, signal) => { ... plannedStopReason = ""; stopSceneRendererLoop(); playoutProcess = null; playoutProcessStartedAtMs = 0; playoutAssetId = ""; ... });   // unconditional

index.ts:4751 (finalize in stopPlayoutProcess): if (playoutProcess === currentProcess) { playoutProcess = null; ... }   // guarded copy

index.ts:4789-4794: const deadline = setTimeout(() => { logRuntimeEvent("playout.stop.deadline_exceeded", ...); finalize(); }, PLAYOUT_STOP_DEADLINE_MS);
```

## [U34] MEDIUM / drift — Video bitrate is implemented three times; at 1080p the GUI, the program feed and the Twitch uplink all use different numbers
**Ort:** apps/worker/src/ffmpeg-runtime.ts:441

**Szenario:** Operator selects a 1080p output profile in the GUI and leaves the bitrate fields empty. The Output page shows 'Follow the server (now: 4500k)' from resolveEncoderQualitySettings(null, env) (page.tsx:20, form:83). Playout encodes the HLS program feed with encoder.maxrate = the flat 4500k default (index.ts:1891) regardless of resolution. The uplink, because rateControlConfigured is false, ignores that value and applies its own resolution ladder: 6000k/12000k for >=1080p (ffmpeg-runtime.ts:441-443). Twitch therefore receives a 6000k re-encode of a 4500k intermediate; the value the operator reads in the GUI is neither what leaves the box nor what the profile implies. Setting any one rate field switches the uplink to the flat trio, so the ladder silently disappears the moment the operator touches e.g. only the audio bitrate.

**Beleg:**
```
ffmpeg-runtime.ts:412-443 getOutputRateControlSettings: if (encoder.rateControlConfigured) { return {...} } ... if (output.height >= 1080 || output.width >= 1920) { return { maxrate: "6000k", bufsize: "12000k", ... } }

index.ts:1891-1892 (getFfmpegCommand): "-maxrate", encoder.maxrate,

packages/core/src/managed-runtime.ts:88: maxrate: maxrate || "4500k",

apps/web/components/encoder-quality-form.tsx:83: placeholder={`Follow the server (now: ${props.fallback.maxrate})`}
```
**Kopien:**
- apps/worker/src/index.ts:1891
- packages/core/src/managed-runtime.ts:88
- apps/web/app/(admin)/output/page.tsx:20
- apps/web/components/encoder-quality-form.tsx:83

## [U35] LOW / drift — Overlay PNG and PiP box are sized for the renderer viewport, the programme video is sized by a separate switch; they can disagree
**Ort:** apps/worker/src/index.ts:1808

**Szenario:** With STREAM_SCALE_ENABLED=0 (a documented knob, .env.example:31), getFfmpegCommand builds no scale/pad filter, so the programme video keeps its source geometry, while the scene PNG is still rendered at getSceneRendererViewport (= output profile size, or SCENE_RENDER_WIDTH/HEIGHT) and composited at overlay=0:0. A 720p profile with a 1080p source puts a 1280x720 overlay in the top-left 2/3 of a 1920x1080 frame; a 1080p profile with a 720p source crops the overlay. resolveSourceLayerPixelBox for the live PiP (index.ts:5007) is computed on the same viewport, so the live window lands where the panel would sit in the viewport, not in the frame. The studio preview renders the payload full-frame at the profile size and shows none of this; the Output page text ('Scene rendering follows the output dimensions unless SCENE_RENDER_WIDTH…') describes the viewport, not the frame.

**Beleg:**
```
index.ts:1808: const outputVideoFilter = isStreamScaleEnabled(process.env) ? getOutputVideoFilter(output) : "";
... `[0:v][${sceneInputIndex}:v]overlay=0:0:format=auto[vout]` when outputVideoFilter is empty

on-air-scene.ts:60-71 getSceneRendererViewport: const width = Number(env.SCENE_RENDER_WIDTH || output.width) || output.width; ... return { width: Math.max(640, width), height: Math.max(360, height) };

index.ts:5007: const viewport = getSceneRendererViewport(process.env, args.outputSettings); const box = resolveSourceLayerPixelBox(args.attach.placement, viewport);
```
**Kopien:**
- apps/worker/src/on-air-scene.ts:60
- apps/worker/src/index.ts:2083
- apps/worker/src/index.ts:5007
- apps/worker/src/output-settings.ts:17

## [U36] HIGH / drift — Blueprint import turns any disabledLayers list into ALL layers (overlay goes blank on air)
**Ort:** apps/web/lib/server/channel-blueprints.ts:277

**Szenario:** Operator exports the channel blueprint (overlay.disabledLayers is []) and imports it again with the Scene studio section ticked. normalizeOverlaySceneLayerOrder([]) (packages/core/src/index.ts:1269-1280) returns the complete list chip,hero,next,queue,schedule,clock,banner,ticker; the blueprint copy of the normaliser has no 'filter back to what was provided' step, so importedLiveOverlay.disabledLayers = all eight. importChannelBlueprint then hands that to updateAppState (line 919) -> normalizeState -> the db normaliser at packages/db/src/index.ts:894, which filters the list against itself and keeps all eight; overlay_settings.disabled_layers_json, the draft (line 927) and every imported preset (line 552) are written with all layers disabled. The core resolver (packages/core/src/index.ts:1719 `enabled: enabledMap[kind] && !disabledLayers.has(kind)`) then draws no layer in the frame piped to ffmpeg, and the studio shows every layer switched off. No warning; tests/unit/channel-blueprints.test.ts:37 feeds disabledLayers: [] and never asserts on the imported value.

**Beleg:**
```
disabledLayers: normalizeOverlaySceneLayerOrder(
  Array.isArray(candidate.disabledLayers) ? candidate.disabledLayers : fallback.disabledLayers
),
```
**Kopien:**
- packages/db/src/index.ts:894 — disabledLayers: normalizeOverlaySceneLayerOrder(overlay.disabledLayers ?? []).filter((kind) => (overlay.disabledLayers ?? []).includes(kind))
- apps/web/app/api/overlay/route.ts:83 — disabledLayers: normalizeOverlaySceneLayerOrder(disabledLayersSource).filter((kind) => disabledLayersSource.includes(kind))
- packages/core/src/index.ts:1696 — normalizeOverlaySceneLayerOrder(disabledLayersSource).filter((kind) => disabledLayersSource.includes(kind))
- packages/core/src/index.ts:1269 — normalizeOverlaySceneLayerOrder appends every missing layer, so a partial list always comes back complete

## [U37] HIGH / silent — updateAssetRecords overwrites every curation column from a snapshot taken before a multi-minute VOD download
**Ort:** packages/db/src/index.ts:5540

**Szenario:** Worker cycle N queues a Twitch VOD download for asset X; vod-cache-jobs.ts request() (line 84) captures the AssetRecord as hydrated at that moment and runJob (line 134-170) hands that same object to onResult after the download, which can take up to TWITCH_VOD_CACHE_DOWNLOAD_TIMEOUT_SECONDS (default 120 s, packages/core/src/managed-runtime.ts:551) or longer. While it runs the operator unticks 'include in programming' for X, or edits its title/tags/folder/chapters (updateAssetCurationRecords / updateAssetMetadataRecords, index.ts:5600/5665). Download finishes -> apps/worker/src/index.ts:787 calls updateAssetRecords([{...asset, cachePath, cacheStatus, ...}]) -> this UPDATE rewrites title, folder_path, tags_json, title_prefix, hashtags_json, platform_notes, chapters_json, chapters_probe_status, include_in_programming, fallback_priority, is_global_fallback from the stale record. The asset silently re-enters rotation with its old title/tags; the operator sees the tick come back and nothing in the audit trail explains it. The other asset writers already guard against this: replaceAssetsForSourceIds (5499-5522) and updateAssetChapterProbeRecords (5739-5753) re-read the row inside the lock; the cache-result path does not.

**Beleg:**
```
UPDATE assets
SET
  title = $2,
  path = $3,
  cache_path = $4,
  cache_status = $5,
  ...
  include_in_programming = $17,
  ...
  is_global_fallback = $23,
  created_at = $24,
  updated_at = $25
WHERE id = $1
```
**Kopien:**
- apps/worker/src/index.ts:787-797 — onResult(asset, result) spreads the captured asset and calls updateAssetRecords
- apps/worker/src/vod-cache-jobs.ts:134-170 — runJob(asset) passes the request-time record to onResult unchanged
- apps/worker/src/index.ts:1588-1609 — same whole-row write from the in-cycle snapshot

## [U38] MEDIUM / drift — queuePreviewCount clamp is 1..5 in two copies and 1..8 in the blueprint copy
**Ort:** apps/web/lib/server/channel-blueprints.ts:274

**Szenario:** Import a blueprint whose sceneStudio.liveOverlay.queuePreviewCount is 7. normalizeChannelBlueprintDocument accepts 7 and importChannelBlueprint returns that normalized document (importedLiveOverlay.queuePreviewCount = 7) to the API caller, so the import result the studio renders says 7. On the write path updateAppState -> normalizeState -> packages/db/src/index.ts:892 clamps to 5; overlay_settings.queue_preview_count = 5; the worker slices the queue to state.overlay.queuePreviewCount (apps/worker/src/index.ts:2783) and five items go on air; the next export writes 5. Same shape as the clamp divergence the operator already found: the number the GUI reports after import is not the number the product uses.

**Beleg:**
```
queuePreviewCount:
  typeof candidate.queuePreviewCount === "number" && Number.isFinite(candidate.queuePreviewCount)
    ? Math.max(1, Math.min(8, Math.round(candidate.queuePreviewCount)))
    : fallback.queuePreviewCount,
```
**Kopien:**
- packages/db/src/index.ts:892 — queuePreviewCount: Math.max(1, Math.min(5, Number(overlay.queuePreviewCount ?? defaults.queuePreviewCount) || defaults.queuePreviewCount))
- apps/web/app/api/overlay/route.ts:81 — queuePreviewCount: Math.max(1, Math.min(5, Number(payload.queuePreviewCount ?? base.queuePreviewCount) || 3))

## [U39] LOW / bug — hasUnpublishedChanges compares updatedAt, so an unchanged draft reads as 'differs from live'
**Ort:** packages/db/src/index.ts:1048

**Szenario:** In the overlay studio press Save draft without touching any field. apps/web/app/api/overlay/route.ts:111 stamps updatedAt = new Date().toISOString() on the draft; saveOverlayDraftRecord (index.ts:6770) and every later readOverlayStudioState (index.ts:5209) compute hasUnpublishedChanges with overlaySettingsEqual, which JSON-stringifies both records including updatedAt (normalizeOverlaySettingsRecord keeps `updatedAt: overlay.updatedAt ?? defaults.updatedAt`, line 900). Live and draft now differ only in the timestamp, so the page shows 'Draft differs from live scene.' (apps/web/app/(admin)/overlay-studio/page.tsx:90) and enables Publish/Reset (apps/web/components/overlay-settings-form.tsx:157-158) although publishing changes nothing on air. The state persists across reloads until publish or reset.

**Beleg:**
```
function overlaySettingsEqual(left: OverlaySettingsRecord, right: OverlaySettingsRecord): boolean {
  const normalizedLeft = normalizeOverlaySettingsRecord(left);
  const normalizedRight = normalizeOverlaySettingsRecord(right);
  return JSON.stringify(normalizedLeft) === JSON.stringify(normalizedRight);
}
```

